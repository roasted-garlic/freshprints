/** Retired compatibility export: legacy catalog tag management is no longer available. */
export function TagManagementModal(_props: {
  isOpen: boolean;
  onClose: () => void;
  onUpdated: () => Promise<void> | void;
}): null {
  return null;
}
