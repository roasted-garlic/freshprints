/**
 * Soft warning threshold for companion set size (Requirement 5 of the companion-sets +
 * explicit-content plan). Staff UI may warn above this many members; there is **no** hard
 * maximum — arbitrary-N companion sets are supported.
 */
export const COMPANION_SET_LARGE_MEMBER_WARNING_THRESHOLD = 10;
