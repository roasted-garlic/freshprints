/**
 * Canonical Fresh Prints external profile / storefront links used by Portal UX.
 * Keep a single source of truth — do not hardcode these URLs in components.
 */

/** Fresh Prints live-show seller profile on Whatnot. */
export const FRESH_PRINTS_WHATNOT_PROFILE_URL =
  'https://www.whatnot.com/user/funkyfreshprints' as const

/** Public Whatnot handle (include @). */
export const FRESH_PRINTS_WHATNOT_HANDLE = '@funkyfreshprints' as const
