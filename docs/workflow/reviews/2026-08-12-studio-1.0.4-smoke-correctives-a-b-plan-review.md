# Review: Studio 1.0.4 smoke correctives A+B (Plan)

| Field | Value |
|-------|-------|
| Date | 2026-08-12 |
| Reviewer | Review Agent |
| Plan | `docs/workflow/plans/2026-08-12-studio-1.0.4-smoke-correctives-a-b-plan.md` |
| Verdict | **approved_with_changes** |

---

## Summary

Plan correctly scopes a 1.0.4 release-smoke amendment for (A) missing Studio import meaningful-transparency validation and (B) missing Firestore archive fast path that surfaces as Firestore `permission-denied`. Root-cause analysis matches repo evidence. Proceed to implementation **only** after the required changes below are followed during implement (no plan rewrite required if implement agent treats them as binding).

---

## Checklist

| Area | Status | Notes |
|------|--------|-------|
| Scope clear and bounded | pass | Explicit IN/OUT; no publish; draft `369535832` frozen |
| Architecture alignment | pass | Main-process validation; Rules fast-path pattern matches approve/reject |
| Security impact addressed | pass | Narrow archive allowlist; fail-closed import; prod Rules gated |
| Data model impact addressed | pass | No new fields; transition docs noted |
| Backend impact addressed | pass | Rules + optional Functions measurement extract |
| Test strategy adequate | pass | Emulator must reproduce deny before fix; fixture tests for A |
| Human checkpoints identified | pass | DEV Rules, DEV QA, ChatGPT merge, prod Rules, rebuild, re-smoke, publish |
| Roadmap alignment | pass | Corrective to active 1.0.4 release, not new feature |
| Documentation plan | pass | Smoke checklist amendment + Rules docs as needed |
| No silent scope expansion | pass | Restore / Portal / Algolia / publish excluded |

---

## Architecture Review

**Findings:**
- Defect A belongs in Electron `validatePngFile` (shared by single/folder/ZIP/multi) — correct choke point before Storage/design/AI.
- Reusing shared `assessMeaningfulTransparency` avoids dual policy. Sharp must stay out of Portal bundles (injected Sharp / Node-only helper) — plan states this.
- Defect B is Rules evaluation path, not `permissionService` — do not “fix” by widening client roles.

**Required changes:**
- [x] During implement: transparency measurement MUST run against **source bytes** and use source `hasAlpha` metadata (Functions pattern). Do not treat post-`ensureAlpha` RGB-as-RGBA as proof of meaningful transparency.
- [x] During implement: keep existing trim/upscale/print-size behavior for **passing** files; transparency failure must short-circuit before Storage.

---

## Security Review

**Findings:**
- Archive fast path must be strictly `hasOnly` the six archive/audit fields with `previousStatus == resource.data.status` and actor UID checks.
- Approval fast path should fail cheap when requested status is `archived` so archive never pays enrichment `diff()` then full validator.

**Required changes:**
- [x] Emulator suite must include DENY cases: non-staff, AI field mutation, wrong `previousStatus`, extra keys.
- [x] Production Rules deploy remains a **separate** human checkpoint after DEV QA.

**Human approval needed before production:**
- [x] Firestore Rules production deploy
- [x] Studio stable draft rebuild / publish (unchanged — still separate)

---

## Data Model Review

**Findings:**
- Soft-archive payload already matches intended lifecycle fields; Rules gap is evaluation path only.

**Required changes:**
- [ ] None beyond documenting the new fast-path constraints if `DATA_MODEL.md` / `SECURITY.md` lack archive transition notes.

---

## Backend Review

**Findings:**
- Functions measurement extract must be behavior-preserving (existing customer-upload tests remain green).
- Studio catalog import has **no** `skipQualityGates` — hard reject opaque (correct per owner).

**Required changes:**
- [x] If Functions files are touched, run Functions build + existing transparency/processing tests in the same PR.

---

## Testing Review

**Findings:**
- Plan correctly requires proving archive **deny** under current Rules with enrichment-heavy fixtures before claiming the fix.
- Import tests must use decoded fixtures (opaque vs transparent), not extension-only stubs.

**Required changes:**
- [x] Add at least one enrichment-heavy `rejected → archived` ALLOW after Rules fix (mirror expression-budget approve tests).
- [x] Confirm existing `designCatalogApproval*` suites still pass after OR-order changes.

---

## Documentation Review

**Findings:**
- Smoke checklist file referenced in the owner prompt was not present at plan time; implement/docs step should create or amend the active 1.0.4 smoke record under `docs/workflow/reviews/`.

**Required changes:**
- [x] Record smoke FAIL for draft `369535832` and that publication remains blocked until A+B re-smoke PASS.

---

## Required Changes (approved_with_changes)

1. Bind implement to Functions-aligned alpha detection (`hasAlpha` + sampled pixels / trim probe) — no color-type-only / extension-only gate.  
2. Archive Rules: cheap-fail or bypass approval `diff()` for `status == "archived"`; strict six-field allowlist + `previousStatus` match.  
3. Emulator: reproduce pre-fix DENY on enrichment-heavy rejected archive, then ALLOW after fix; keep approval suites green.  
4. Document smoke FAIL / no-publish for draft `369535832` in workflow reviews.  
5. No implementation of publish, draft mutation, Intel/Windows owner smoke, or production Rules until their separate authorizations.

---

## Blockers

None — plan is sufficient to implement under the binding changes above.

---

## Verdict Rationale

Repo evidence supports both defect diagnoses. Scope, security posture, and DEV-first sequencing are adequate. Conditional approval encodes measurement-order and Rules/emulator hardening so implement does not weaken either corrective.

---

## Next Step

Implementation may begin on a narrow hotfix branch from `4ac77141f0f12845e1ca11b742c5242488496b70` after owner sends **Continue Workflow** (or explicit implement authorization). **STOP now** — no code changes in this Formal Review turn.
