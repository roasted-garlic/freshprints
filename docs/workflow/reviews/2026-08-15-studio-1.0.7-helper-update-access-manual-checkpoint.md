# Human Checkpoint — Studio 1.0.7 Helper Update Access (DEV QA)

| Field | Value |
|-------|-------|
| Date | 2026-08-15 |
| Workflow | managed-phase / Test / `studio-1.0.7-helper-update-access` |
| Reason | Plan-required owner DEV QA: Helper can use Studio Updates without Settings |
| Status | **resolved** |
| Resolution | Owner **PASS** (2026-08-15) |

---

## What We Need From You

Confirm in DEV (or packaged Studio) that Helpers can open Studio Updates from the sidebar footer while still being blocked from Settings.

---

## Context

Implement shipped a footer **Studio Updates** action gated by `permissionService.canAccessDesktopApp(user)`, opening a thin modal that reuses `StudioUpdatesSettingsSection`. Settings remains `manageSettings`-only. Automated checks passed; Signoff waits on this QA.

Modal is `modal-panel-lg` so release notes have room.

---

## Manual Test Required

**Feature / area:** Studio Updates sidebar access for Helpers  
**Environment:** local DEV Studio (Electron) preferred; packaged optional  
**Prerequisites:**
- Helper account (active helper role; no `manageSettings`)
- Owner or admin account
- Studio running against the 1.0.7 implement tree

### Steps

1. Sign in as **Helper** → **Expected:** Sidebar footer shows **Studio Updates**; **Settings** nav item is absent.
2. Click **Studio Updates** → **Expected:** Modal opens with existing updater UI (current version / check controls).
3. Close the modal (X or overlay) → **Expected:** Closes normally; shell usable.
4. As Helper, try to open Settings (nav if visible, or navigate to `/settings`) → **Expected:** No Settings access (nav hidden and/or route blocked).
5. Sign in as **Owner or Admin** → **Expected:** Footer **Studio Updates** still visible; **Settings** still available.
6. Owner/Admin: open **Settings** → confirm Studio updates section still present; optionally open footer modal too → **Expected:** Both paths show the same updater controls.

### Pass criteria

- [ ] Helper sees Studio Updates and can open/close the modal
- [ ] Helper cannot access Settings
- [ ] Owner/admin still see Studio Updates (footer) and Settings → updates
- [ ] No obvious updater breakage vs Settings path

### Please reply with

- `PASS` — all criteria met
- `FAIL: [description]` — what failed
- `PASS WITH NOTES: [notes]` — acceptable with follow-ups

**Your result:** **PASS**

---

## Impact If Delayed

N/A — resolved.

---

## Agent Actions While Paused

N/A — resolved; Signoff completed.

---

## Resolution Record

| Date | User response | Recorded in state | Follow-up |
|------|---------------|-------------------|-----------|
| 2026-08-15 | `PASS` | yes | Signoff **approved** |

---

## Resume Checklist

- [x] User feedback recorded in `.cursor/workflow/state.md` Decision Log
- [x] `Human Checkpoint Required` set to `no`
- [x] Test report manual section updated
- [x] Signoff complete (`docs/workflow/reviews/2026-08-15-studio-1.0.7-helper-update-access-signoff.md`)
