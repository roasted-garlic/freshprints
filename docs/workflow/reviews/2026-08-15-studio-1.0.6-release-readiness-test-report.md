# Test Report: Studio 1.0.6 Release Readiness (A2 declined)

| Field | Value |
|-------|-------|
| Date | 2026-08-15 |
| Tester | Test Agent |
| Goal | `studio-mac-autoupdate-signing-and-searchable-category-picker` |
| Target | Studio **1.0.6** |
| Plan | `docs/workflow/plans/2026-08-14-studio-mac-autoupdate-signing-and-searchable-category-picker-plan.md` |
| Amendment | `docs/workflow/plans/2026-08-15-studio-1.0.6-a2-declined-release-without-apple-program-plan-amendment.md` |
| Amendment Review | `approved_with_changes` — `docs/workflow/reviews/2026-08-15-studio-1.0.6-a2-declined-release-without-apple-program-plan-review.md` |
| Candidate SHA | `095107549069cddc18a754fa17f83047fe718472` (`origin/development`, PR #76 tip) |
| Production lineage | `9f945f3c2435f1e6939e250c435ca5e4dc503201` (PR #77) — contains candidate tip |
| Overall | **passed_with_notes** |

---

## Summary

Studio **1.0.6** release-candidate source at `0951075` passes all required automated checks for A1, B, C-SHARED, D, updater/signing policy, Studio typecheck/lint/Vite build, and Functions build. **A2 Developer ID remains DECLINED** (ADR-FP-136); Mac stays ad-hoc / `internal-unsigned`; Mac auto-update **install** remains unsupported; Windows updater policy unchanged. No production mutation and no Studio release dispatch occurred during Test.

---

## Lineage

| Ref | SHA | Notes |
|-----|-----|-------|
| Test candidate | `0951075` | `origin/development` — Merge PR #76 |
| Production tip | `9f945f3` | Merge PR #77; `git merge-base --is-ancestor 0951075 origin/production` → true |
| Signed-off product commit | `0451bc4` | Ancestor of candidate |
| App/code vs `origin/development` | clean | No unexplained product diff under `apps` / `functions` / `packages` / `.github` |
| Studio version | `1.0.6` | `apps/studio/package.json` + finalize pin |

Uncommitted local docs (A2 decline amendment artifacts, C-SHARED promotion records, state) are workflow documentation only — not product code.

---

## Workstream evidence (pre-Test)

| Stream | Status | Artifacts |
|--------|--------|-----------|
| A1 updater install error UX | Implemented + unit/contract | Impl review 2026-08-14; `studioUpdateErrorMapping` + sticky install context tests |
| A2 Developer ID | **DECLINED indefinitely** | ADR-FP-136; credential checkpoint DECLINED |
| B searchable Category | Implemented; owner **B PASS** | Owner QA checklist; corrective checklist notes B PASS · D PASS |
| C-SHARED Staff Gang Sheets | Signoff **approved_with_notes**; prod backend complete | C-SHARED test/signoff/deploy/smoke |
| D AI Review thumbnail bg | Implemented; owner **D PASS** | C+D impl review; corrective checklist |

---

## Commands Run

| Check | Command | Exit | Result | Notes |
|-------|---------|------|--------|-------|
| Studio typecheck | `npx tsc --noEmit` (cwd `apps/studio`) | 0 | pass | |
| Lint | `npm run lint` | 0 | pass | |
| Studio Vite build | `node scripts/generate-packaged-build-config.mjs && npx vite build` (cwd `apps/studio`) | 0 | pass | Renderer + electron main + preload |
| Functions build | `npm --prefix functions run build` | 0 | pass | Also previously proven on prod deploy of `9f945f3` |
| Focused unit/contract (RC set) | `npx tsx --test` (25 files; see below) | 0 | pass | **139** tests |
| A1 error mapping | `npx tsx --test packages/shared/src/studioUpdate/studioUpdateErrorMapping.test.ts` | 0 | pass | **9** tests |
| Hygiene | `git diff --check` | 0 | pass | |
| Firestore rules emulator | — | — | skip | Java missing on agent host (same as C-SHARED Test) |
| Full monorepo unit sweep | — | — | skip | Scoped to 1.0.6 surfaces |
| E2E | — | — | skip | Not configured |
| Electron-builder package / live Mac codesign | — | — | skip | CI packaging; local Mac not required for this Test |
| Intentionally failing Mac auto-update install | — | — | skip | Limitation already established; amendment does not require repro |

**Focused RC file set (139 tests):** searchable Category contracts/helpers; AI Review / artwork background contracts; Staff Gang Sheet shared + Studio + Functions contracts; `studioUpdate*` IPC/service/channel; `write-studio-release-env`; `studioUpdateReleaseNotes`; `.github/workflows/studio-release-signing-policy.test.ts`.

**Total automated tests this phase:** 139 + 9 = **148 pass / 0 fail**.

---

## A2 decline consistency

| Assertion | Result |
|-----------|--------|
| No required `MAC_CSC_*` for successful Mac `internal-unsigned` path | **pass** — secrets only referenced in fail-closed error when `signed` is selected |
| Stable Mac path does not claim Developer ID | **pass** — warnings state ad-hoc, not Developer ID, not notarized |
| `electron-builder` Mac `identity: "-"`, `hardenedRuntime: false` | **pass** |
| No notarization requirement introduced | **pass** — no `notarize` in builder; workflow text forbids claiming notarization |
| Signing-policy tests agree with declined-A2 behavior | **pass** — still require `internal-unsigned` for stable Mac; reject `signed` without Apple secrets |
| Docs do not claim Mac auto-update install fixed | **pass** — DEPLOYMENT / ROADMAP / ADR-FP-136 keep limitation open |
| A1 manual-install fallback intact | **pass** — install-context → safe “could not be installed… install … manually” |
| Windows updater policy unchanged | **pass** — Windows CSC / NSIS / silent install contracts still green |
| Squirrel validation not weakened | **pass** — no bypass code |

### Accepted note (non-blocking)

CI Mac fail-closed copy still says “until … MAC_CSC_* … (Workstream A2 gated)”. Behavior matches ADR-FP-136 (signed Mac blocked; ad-hoc only). Wording implies a future credential phase rather than “declined indefinitely” — **messaging debt only**; not a release blocker and **not** fixed during Test.

---

## Platform results

| Area | Result |
|------|--------|
| Windows updater | **Unchanged / pass** (contracts + signing decision matrix) |
| Mac manual-install packaging policy | **Valid / pass** (ad-hoc identity, arm64+x64 dmg+zip, codesign verifier expects adhoc, finalize asset triad) |
| Mac auto-update install | **Unsupported** (accepted limitation; not marked fixed) |

---

## Manual QA

| Class | Item | Status |
|-------|------|--------|
| **REQUIRED** | New owner product QA for B / D / C-SHARED | **None** — already recorded PASS / C-SHARED Signoff |
| **OPTIONAL** | Windows update check/install smoke | **not performed** this Test |
| **OPTIONAL** | Mac manual DMG + Open Anyway | **not performed** this Test |
| **OPTIONAL** | Mac Settings updater; install fail → A1 copy | **not performed** this Test |

Optional smoke may still be useful before publish but is **not** a Test gate per approved A2-decline amendment.

---

## Known accepted limitations

1. Mac automatic update **install** unreliable under ad-hoc signing (open).
2. A2 / paid Apple Developer Program declined indefinitely.
3. Firestore rules emulator not run on this host (Java).
4. CI Mac gate message still says “A2 gated” (stale phrasing; behavior correct).

---

## Failures

None.

---

## Signoff Readiness

- [x] All required automated checks pass
- [x] No unexplained release-candidate product diff
- [x] A1 / B / C-SHARED / D coverage green
- [x] Windows updater unchanged; Mac manual path valid; A2 declined reflected
- [x] No production mutation; no Studio dispatch
- [x] Ready for **Signoff** phase

**Next step:** `Continue Workflow` → FreshForge **Signoff** only. **STOP before** Studio 1.0.6 dispatch/publish until Signoff + owner release authorization phrase.
