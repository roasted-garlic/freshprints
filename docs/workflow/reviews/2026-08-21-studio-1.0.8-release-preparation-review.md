# Review: Prepare Studio 1.0.8 release from production `97d6d49`

| Field | Value |
|-------|-------|
| Date | 2026-08-21 |
| Reviewer | Review Agent |
| Plan | docs/workflow/plans/2026-08-21-studio-1.0.8-release-preparation-plan.md |
| Verdict | **approved** |

---

## Summary

Owner named Studio **1.0.8** from production tip `97d6d49`. That SHA is verified and still **1.0.7**, so dispatch from it is correctly forbidden. Pin surfaces match the 1.0.7 release-tooling contract. Gated implement → PR → merge → dispatch from the **new** SHA → publish is the right order. This pass must not bump version or dispatch.

---

## Checklist

| Area | Status | Notes |
|------|--------|-------|
| Scope clear and bounded | pass | Three pin files + later Git/dispatch. No product reimplementation. |
| Architecture alignment | pass | No layer changes. Updater uses new semver. |
| Security impact addressed | pass | A2 not reopened. `internal-unsigned`. No secrets. |
| Data model impact addressed | pass | None. |
| Backend impact addressed | pass | No Firebase. App Hosting traffic stays `7716d4a`. |
| Test strategy adequate | pass | Signing-policy test + Studio tsc/lint/Vite; installer in CI; packaged smoke after draft. |
| Human checkpoints identified | pass | Implement, PR create, PR merge, dispatch, publish. |
| Roadmap alignment | pass | Ships polish + already-merged Print Request Studio Git. Phase 9 PARKED. |
| Documentation plan | pass | ROADMAP/handoff; no DEPLOYMENT rewrite. |
| No silent scope expansion | pass | Will not full-lockfile churn, A2, Portal, or parked untracked docs. |

---

## Architecture Review

**Findings:**

- Product baseline is PR #85 merge `97d6d49` (includes #84 Studio Print Request work + polish).
- Version bump must land on `development` then `production`; release SHA ≠ `97d6d49`.

**Required changes:**

- [ ] None

---

## Security Review

**Findings:**

- Mac gate numeral-only; keep fail-closed on `signed` without A2 credentials.
- Stable dispatch still requires production ancestry.

**Required changes:**

- [ ] None

**Human approval needed before production:**

- [x] Version-pin PR create and merge
- [x] Release dispatch
- [x] GitHub publish after smoke

---

## Data Model Review

**Findings:** None.

**Required changes:**

- [ ] None

---

## Backend Review

**Findings:**

- No Functions/Rules/indexes. Do not App Hosting this pin merge.

**Required changes:**

- [ ] None

---

## Testing Review

**Findings:**

- Signing-policy test must be updated with the pin or CI will fail.
- Packaged smoke must cover list-split (absent from 1.0.7) plus polish.

**Required changes:**

- [ ] None

---

## Documentation Review

**Findings:**

- Plan correctly forbids dispatch from `97d6d49` and 1.0.7 rebuild.

---

## Required Changes (if approved_with_changes)

None.

---

## Blockers (if blocked)

None.

---

## Verdict Rationale

**approved.** Baseline SHA, version number, pin files, and gate phrases are sufficient. Do not implement until the owner implement phrase.

---

## Next Step

**STOP.** Await:

```text
APPROVE IMPLEMENT: studio-1.0.8-release-preparation
```

Do not bump version, open a production PR, dispatch `studio-release.yml`, or publish until the matching later phrases.
