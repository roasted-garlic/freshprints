# Review: OCR arched text + Re-run overlay stepper

**Date:** 2026-06-25
**Status:** approved

One-shot OCR retry with `low` reasoning on implausible visibleText or confidence < 0.75. Description `/` fallback before logging low quality. Overlay uses same optimistic stage pattern as Processing tab.
