# Checkpoint: Studio v1.0.0 Final Release Refresh Preparation

Date: 2026-08-04
Active managed goal: `production-release`
Scope: pre-flight verification for a corrected internal-unsigned Studio v1.0.0 stable release run
from the exact final production commit, after owner QA confirmed PASS for PR #37, #38, and #39.

## Verdict: ALL SAFE VERIFICATION COMPLETE — STOPPED AT THE MANUAL WORKFLOW-DISPATCH CHECKPOINT

No hard-stop condition was triggered. Every command run in this pass succeeded. The workflow was
**not** triggered, because this environment has no authenticated GitHub Actions dispatch method
(no `gh` CLI installed, no `GITHUB_TOKEN`/`GH_TOKEN` in the environment) — per the task's own explicit
instruction, this is not treated as a failure requiring improvisation; the exact manual dispatch
steps are provided below for the owner.

## 1. Starting-state verification

```
git status --porcelain                    -> (empty; clean, before this pass began)
git fetch origin
git switch production
git pull --ff-only origin production       -> fast-forwarded e1e83ae..70c083a (exactly PR #39's content)
git rev-parse HEAD                          -> 70c083af6ec0165e95f439fe6111e7e0a62c8ecd
git rev-parse origin/production             -> 70c083af6ec0165e95f439fe6111e7e0a62c8ecd
git log -1 --format="%H %s" 70c083a...      -> "Merge PR #39: prevent stale print-request rows during tab switches"
```

**Both `HEAD` and `origin/production` confirmed at exactly `70c083af6ec0165e95f439fe6111e7e0a62c8ecd`.**
PR #39 confirmed merged (this exact commit *is* the PR #39 merge commit).

- `apps/studio/package.json`: `"version": "1.0.0"` — confirmed unchanged.
- `.github/workflows/studio-release.yml` — confirmed, via direct source re-inspection on this exact
  commit:
  - `workflow_dispatch:` present (manual-only trigger, no `push`/`schedule` trigger).
  - "Guard stable release ref" step + `git merge-base --is-ancestor HEAD origin/production` — the
    stable-source restriction is intact.
  - `distribution_mode` input present with `signed`/`internal-unsigned` options, default `signed`.
  - All six `PROD_FIREBASE_*` secrets referenced in the "Configure Studio Firebase environment" step,
    fail-closed on any missing value (unchanged logic, re-confirmed by source read).
  - electron-builder's GitHub publisher (`npm run build -- --publish always`) creates a **draft**,
    never auto-published — confirmed via the workflow's own explanatory comments, re-read in full.

No divergence found anywhere in this section. Proceeding was safe.

## 2. Owner QA and remediation signoff

Created: `docs/workflow/reviews/2026-08-04-production-studio-print-request-release-blockers-signoff.md`

Records, for each of PR #37 (`2d2697d022a551fc33bfc1815843e5fa7cfdfa3a`), PR #38
(`e1e83ae5db447f996490e2edab8578717a068d9a`), and PR #39
(`70c083af6ec0165e95f439fe6111e7e0a62c8ecd`): confirmed merged, owner QA **PASS**, exact Plan/Review/
Test-Report/Implementation-Review document paths, and confirmation the `v1.0.0` draft Release
remained unpublished throughout. No application source was modified to produce this document.

## 3. Read-only GitHub Release and tag audit

No `gh` CLI and no `GITHUB_TOKEN`/`GH_TOKEN` available in this environment. Used the repository's
already-public status (confirmed in prior-session memory) to query GitHub's **unauthenticated**
public REST API, which is sufficient for tags (never gated) but **not** sufficient to positively
confirm or deny a draft release's existence (drafts are excluded from every unauthenticated releases
endpoint by GitHub's own access model) — this limitation is reported honestly below rather than
guessed around.

| Check | Result |
|---|---|
| Does a `v1.0.0` Git tag exist? | **No.** `git tag -l "v1.0.0*"` and `git ls-remote --tags origin` both list only `v1.0.0-beta.1` through `.5` and `v1.0.0-rc1` through `.5` — no plain `v1.0.0` tag anywhere, local or remote. Cross-checked via GitHub's public Tags API (`GET /repos/roasted-garlic/freshprints/tags`, unauthenticated, never gated by draft status) — same result: no `v1.0.0` tag. |
| What commit does the `v1.0.0` tag resolve to? | N/A — the tag does not exist. |
| Does a draft GitHub Release exist for `v1.0.0`? | **Could not be positively confirmed via unauthenticated API** (`GET /repos/.../releases`, `GET /repos/.../releases/tags/v1.0.0` → 404, `GET /repos/.../releases/latest` → 404 — all three are consistent with either "no release exists" or "a draft exists but is invisible without auth"). **However**, GitHub's release-creation API always creates the underlying Git tag at the same time a release (draft or published) is created — this is a real, documented GitHub API behavior, not project-specific. Since the public Tags API authoritatively shows no `v1.0.0` tag exists (tags are never draft-gated), this is strong indirect evidence that **no `v1.0.0` draft release currently exists either.** This is inference from authoritative tag data, not a guess about API behavior we could not verify. |
| Release ID / target commitish / draft status / prerelease flag / assets | Not applicable — no evidence a `v1.0.0` release object exists to have these properties. |
| Do existing installer/blockmap/`latest.yml` belong to the earlier pre-remediation commit? | Not applicable for `v1.0.0` — no such release/assets were found. (The publicly visible pre-release assets, e.g. `Fresh-Prints-Windows-1.0.0-beta.5-Setup.exe` under the `v1.0.0-beta.5` release, are unrelated prerelease artifacts from `development`, not `production`, and are not proposed for any change here.) |
| Can rerunning electron-builder safely update/create the draft? | **Yes, with high confidence** — if no `v1.0.0` release/tag currently exists (as the tag evidence indicates), triggering the workflow will **create** a new draft and a new `v1.0.0` tag, not move, delete, or overwrite anything. This is a first stable run, not a "refresh" of prior conflicting state. No tag move, no release deletion, and no asset conflict is implied by anything found in this audit. |

**No tag move, tag deletion, release deletion, or old-asset deletion appears necessary.** The
`APPROVE V1.0.0 DRAFT RELEASE AND TAG REFRESH` checkpoint phrase is **not** being requested, since no
evidence of a destructive-cleanup requirement was found — this differs from what the task brief's
framing anticipated ("refresh" implying an existing draft), and that discrepancy is called out
explicitly here rather than silently assumed away. If the owner has independent, direct knowledge
(via the GitHub UI, which they can see and this environment cannot) that a `v1.0.0` draft *does*
exist despite the tag evidence, they should say so before the workflow is dispatched — in that case
the situation reduces to a normal "rerun creates additional assets on the existing draft" case, which
electron-builder's GitHub publisher handles natively (it uploads new assets to an existing draft with
the same tag, it does not delete the draft or move the tag), still requiring no destructive action.

## 4. Full final-source verification (all on commit `70c083af6ec0165e95f439fe6111e7e0a62c8ecd`)

| Check | Command | Result |
|---|---|---|
| Root dependency install | `npm ci` | exit 0 |
| Functions dependency install | `npm ci` (in `functions/`) | exit 0 |
| Functions build | `npm run build` (in `functions/`) | exit 0 |
| Portal typecheck | `npx tsc --noEmit` (in `apps/portal/`) | exit 0 |
| Studio packaged build config generation | `node scripts/generate-packaged-build-config.mjs` | exit 0 — **channel: `stable`** (confirmed via script output) |
| Studio typecheck | `npx tsc --noEmit` (in `apps/studio/`) | exit 0 |
| Repo lint | `npm run lint` | exit 0, 0 warnings |
| Studio updater tests (exact release-workflow command) | `npx tsx --test packages/shared/src/studioUpdate/studioUpdateStateTransitions.test.ts packages/shared/src/studioUpdate/studioUpdateErrorMapping.test.ts apps/studio/electron/ipc/studioUpdate/studioUpdateChannel.test.ts apps/studio/electron/ipc/studioUpdate/studioUpdateIpcChannels.test.ts apps/studio/scripts/generate-packaged-build-config.test.ts` | **23/23 pass** |
| PR #37/#38/#39 focused regression tests (7 files) | `npx tsx --test <7 files>` | **50/50 pass** |
| Studio production build (non-packaging) | `tsc && vite build` (renderer, main, preload) | exit 0, all three bundles built; pre-existing chunk-size/dynamic-import warnings unrelated |
| `git diff --check` | — | exit 0 |

### Additional required verifications

- **No dev-only Test Data tools can ship in production:** confirmed all three protection layers
  present and unmodified on this exact commit — (1)
  `apps/studio/src/renderer/src/features/test-data-reset/utils/operationalWipeUiGate.ts`'s
  `import.meta.env.DEV` build-time gate, (2) its `OPERATIONAL_WIPE_ALLOWED_PROJECT_IDS` runtime
  project allowlist, (3) `wipeOperationalTestData`'s confirmed exclusion from the already-deployed
  production Cloud Functions allowlist (per the historical, still-valid
  `docs/workflow/reviews/2026-07-30-production-release-functions-allowlist-report.md` — Functions
  are not being redeployed in this pass, so this exclusion's live status is unchanged from that prior
  verification).
- **Generated packaged build channel is `stable`:** confirmed directly in the generation script's own
  output (`[generate-packaged-build-config] Wrote packaged update channel "stable"`).
- **Build source contains all three Print Requests remediations:** confirmed via direct `grep` for
  each remediation's own marker function on this exact commit — PR #37
  (`resolveShowQueuePrintRequestLinkTab` in `showQueuePrintRequestSources.ts`), PR #38
  (`mergePrintRequestsById` in its own file; `resolvePrintRequestItemArtworkBackground` wired into
  `PrintRequestItemCard.tsx`), PR #39 (`derivePrintRequestsListLoading` in `usePrintRequests.ts`;
  `filterPrintRequestsByActiveTab` wired into `PrintRequestsPage.tsx`).
- **No source modification was required:** confirmed — this entire pass performed verification only;
  `git status --porcelain` shows only the two new workflow-artifact documents this pass created, zero
  application source changes.
- **No Firebase deployment occurred or was required:** confirmed — no `firebase deploy` command was
  run anywhere in this pass; no Firestore Rules/index/Functions/Storage/App Hosting action of any
  kind was performed.

## 5. Production Firebase secret readiness — NOT VERIFIABLE FROM THIS ENVIRONMENT

This environment has no GitHub API authentication of any kind (`GET
/repos/roasted-garlic/freshprints/actions/secrets` correctly returns `401` even for a public repo —
secret listings always require write/admin auth, regardless of repo visibility). **Presence/nonempty
status of the six required `PROD_FIREBASE_*` secrets cannot be confirmed by this coding agent.** Per
the standing hard security rule (never request, receive, print, or infer secret values), this must be
confirmed directly by the owner via GitHub → Settings → Secrets and variables → Actions → Repository
secrets, checking only that all six exist:

- `PROD_FIREBASE_API_KEY`
- `PROD_FIREBASE_AUTH_DOMAIN`
- `PROD_FIREBASE_PROJECT_ID`
- `PROD_FIREBASE_STORAGE_BUCKET`
- `PROD_FIREBASE_MESSAGING_SENDER_ID`
- `PROD_FIREBASE_APP_ID`

If all six already existed and were verified before the original Phase G stable run (per prior
session history in `.cursor/workflow/state.md`), their continued presence has not changed as a result
of anything in this pass — but this environment cannot re-confirm that fact live, and the task
requires it be stated plainly rather than assumed.

## 6. Windows signing configuration state — NOT VERIFIABLE FROM THIS ENVIRONMENT (same reason)

`WINDOWS_CSC_LINK` and `WINDOWS_CSC_KEY_PASSWORD` secret presence/absence likewise cannot be checked
from this environment. The workflow's own "Configure optional Windows signing" step (re-read in full
on this exact commit, `.github/workflows/studio-release.yml:163-193` and beyond) already fails closed
on a half-configured pair at run time via `$hasLink -xor $hasPassword` — this logic is confirmed
present and unmodified, so **if** a half-configured pair exists, the workflow run itself will fail
loudly rather than silently proceeding; this pass cannot independently pre-confirm the pair's state,
but the workflow's own fail-closed behavior is the correct backstop regardless.

## 7. Conditional workflow execution — NOT TRIGGERED

Per the task's required conditions:

1. Exact production SHA verified — ✅ (§1)
2. All tests and builds pass — ✅ (§4)
3. All required production Firebase secrets present — **cannot be confirmed by this environment** (§5)
4. Signing-secret pair not half-configured — **cannot be confirmed by this environment** (§6); the
   workflow's own fail-closed logic is intact as a backstop
5. Release/tag audit proves the workflow can run without destructive cleanup — ✅, with the
   important honesty caveat in §3 about unauthenticated-API visibility limits
6. An authenticated workflow-dispatch method is available — **✗ — no `gh` CLI, no
   `GITHUB_TOKEN`/`GH_TOKEN` found in this environment**

Condition 6 is not met. Per the task's explicit instruction ("If no authenticated CLI or API dispatch
method is available, do not improvise. Return the exact GitHub Actions UI steps and wait for the
owner to trigger it."), **the workflow was not triggered.** This is not treated as a failure of this
pass — every command that *could* be run, was run, and passed.

### Exact GitHub Actions UI steps for the owner to trigger the run

1. Go to `https://github.com/roasted-garlic/freshprints/actions/workflows/studio-release.yml`.
2. Click **Run workflow**.
3. Set the branch/ref dropdown to the exact commit `70c083af6ec0165e95f439fe6111e7e0a62c8ecd` (or
   type it directly if the UI allows a SHA, otherwise select `production` — since this commit *is*
   `origin/production`'s current `HEAD`, selecting the `production` branch resolves to this exact
   commit as long as no further commits have landed on `production` between this checkpoint and the
   dispatch).
4. Set **release_type** to `stable`.
5. Set **distribution_mode** to `internal-unsigned`.
6. Click **Run workflow**.
7. Before dispatching, the owner should independently verify (via GitHub Settings, not via this
   agent) that all six `PROD_FIREBASE_*` secrets are present and that `WINDOWS_CSC_LINK`/
   `WINDOWS_CSC_KEY_PASSWORD` are either both present or both absent.

## 8. Workflow verification — NOT APPLICABLE (no run was triggered)

No workflow run exists to verify. This section will be completed in a follow-up pass once the owner
triggers the run and reports the run ID/URL, or grants this environment an authenticated dispatch
method.

## 9. Required release assets — NOT APPLICABLE (no new run has occurred)

No new installer, blockmap, or `latest.yml` was produced by this pass. The expected filenames,
once a run does complete, remain unchanged from the workflow's own naming convention:
`Fresh-Prints-Windows-1.0.0-Setup.exe`, its matching `.blockmap`, and `latest.yml` — confirmed via the
workflow source's electron-builder configuration reference, not renamed or altered by any commit in
this remediation sequence.

## Confirmation

No GitHub Release was published. No tag was created, moved, or deleted. No GitHub Actions workflow
was dispatched. No installer was built. No Firebase resource was deployed or modified. No production
data was touched. No custom domain action occurred. No application source code was modified — this
entire pass performed verification and documentation only. The two new documents created in this pass
(`docs/workflow/reviews/2026-08-04-production-studio-print-request-release-blockers-signoff.md` and
this file) are the only filesystem changes made.
