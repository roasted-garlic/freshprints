# Signoff: Studio 1.0.6 — Mac auto-update signing goal (A2 declined) + searchable Category + Staff Gang Sheets + AI Review bg

| Field | Value |
|-------|-------|
| Date | 2026-08-15 |
| Signoff by | Signoff Agent |
| Goal | `studio-mac-autoupdate-signing-and-searchable-category-picker` |
| Target | Studio **1.0.6** |
| Plan | `docs/workflow/plans/2026-08-14-studio-mac-autoupdate-signing-and-searchable-category-picker-plan.md` |
| A2 amendment | `docs/workflow/plans/2026-08-15-studio-1.0.6-a2-declined-release-without-apple-program-plan-amendment.md` |
| A2 Review | `approved_with_changes` — `docs/workflow/reviews/2026-08-15-studio-1.0.6-a2-declined-release-without-apple-program-plan-review.md` |
| Test report | `docs/workflow/reviews/2026-08-15-studio-1.0.6-release-readiness-test-report.md` → **passed_with_notes** |
| Tested candidate SHA | `095107549069cddc18a754fa17f83047fe718472` |
| Production / release source SHA | `9f945f3c2435f1e6939e250c435ca5e4dc503201` |
| Final status | **approved_with_notes** |

---

## Summary

Managed goal product work for Studio **1.0.6** is complete and eligible for release preparation under the accepted A2 decline.

| Workstream | Outcome |
|------------|---------|
| **A1** | Install-phase updater error UX complete — install failures map to safe manual-install guidance |
| **A2** | **DECLINED / DEFERRED INDEFINITELY** (ADR-FP-136) — no paid Apple Developer Program, no `MAC_CSC_*`, no notarization |
| **B** | Searchable Category picker complete; owner QA **PASS** |
| **C-SHARED** | Shared Staff Gang Sheets / Internal Sheets complete; production backend promoted; Whatnot/Portal boundaries preserved; prior C-SHARED Signoff **approved_with_notes** |
| **D** | AI Review selected-thumbnail artwork background sync complete; owner QA **PASS** |

**Windows** automatic updates remain supported and unchanged.  
**Mac** remains ad-hoc / `internal-unsigned`; manual install is the supported path; automatic update **install** remains unsupported/unreliable. Do **not** claim the Mac signing / auto-update-install issue was fixed.

This Signoff **approves release readiness**. It does **not** authorize dispatch or publish. **STOP before** Studio 1.0.6 workflow dispatch / GitHub release creation / publish until the owner phrase below is given.

---

## Release contract (binding)

### A1
- Install-phase updater error UX complete.
- Sticky `activeErrorContext = "install"` before `quitAndInstall`.
- Safe copy on install failure: update downloaded but could not be installed → install latest Fresh Prints version manually.

### B
- Searchable Category on Design edit + AI Review Category only.
- Placement / Design Library filter remain non-searchable.
- Owner QA **PASS**.

### C-SHARED
- Shared Internal Gang Sheets (`source: "staff_gang_sheet"`).
- Production backend promotion complete (Rules + indexes + allowlisted Functions on `fresh-prints-prod` @ `9f945f3`).
- Whatnot Show Queue and Portal isolation preserved.

### D
- AI Review left-rail / selected thumbnail uses same `artworkBackgroundHex` resolve path as workspace.
- Owner QA **PASS**.

### A2 (declined)
- No Developer ID Application certificate.
- No `MAC_CSC_LINK` / `MAC_CSC_KEY_PASSWORD`.
- No notarization.
- No Squirrel.Mac validation bypass, Gatekeeper disable, self-signed “production” cert, or fake Team ID.
- Mac auto-update install limitation remains **open**.

### Windows
- Automatic update check → download → restart-and-install **supported / unchanged**.

### Mac
- arm64 + x64 builds remain ad-hoc / `internal-unsigned`.
- Manual DMG / Open Anyway is the supported install path.
- Automatic update check/download may still run; **install** unsupported/unreliable under ad-hoc.
- A1 manual-install guidance is the Settings fallback.

---

## Lineage verification (Signoff)

| Check | Result |
|-------|--------|
| Tested candidate | `0951075` (`origin/development`) |
| Production tip | `9f945f3` (PR #77) |
| Production contains candidate | **yes** (`merge-base --is-ancestor`) |
| Product tree `0951075` vs `9f945f3` (`apps`/`functions`/`packages`/`.github`) | **identical** (no unexpected product changes) |
| Studio version | **1.0.6** |
| Release source for workflow | **`9f945f3`** on `production` (production-gated stable policy) |
| Mac distribution for stable | **`internal-unsigned`** |
| Artifact matrix | Windows + Mac x64 + Mac arm64 intact (signing-policy tests) |

Uncommitted local workflow/docs do **not** alter the app/runtime candidate.

---

## Changes Delivered

### Behavior
- Searchable Categories in Studio design edit and AI Review
- Shared Staff / Internal Gang Sheets workflow (Studio + Functions + Rules)
- AI Review thumbnail background synchronization with selected preview mat
- Updater install-failure guidance (A1)
- Explicit product constraint: Mac Developer ID not pursued (docs/ADR)

### Documentation (local / to commit on development)
- A2 decline plan amendment + Formal Review + credential checkpoint DECLINED
- ADR-FP-136; DEPLOYMENT Mac/A2 section; ROADMAP banner
- C-SHARED production promotion records / smoke
- Release-readiness Test report; this Signoff
- Workflow state + ChatGPT handoff CURRENT-STATE / recent work

### Explicitly not delivered
- A2 Developer ID signing / notarization
- Mac auto-update install reliability
- Studio 1.0.6 GitHub release / workflow run (gated)

---

## Tests

### Automated (carried from Test — not re-run at Signoff)

| Check | Exit / count |
|-------|----------------|
| Studio typecheck | 0 |
| Lint | 0 |
| Vite build | 0 |
| Functions build | 0 |
| Focused RC tests | 139 pass |
| A1 error mapping | 9 pass |
| **Total** | **148 pass / 0 fail** |
| `git diff --check` | 0 |

### Skipped (not passes)

| Check | Reason |
|-------|--------|
| Firestore rules emulator | Java unavailable |
| Full monorepo unit sweep | Out of scope |
| E2E | Not configured |
| Live Mac packaging/codesign | CI path |
| Intentional Mac auto-update install failure | Unnecessary; limitation established |

### Manual

| Test | Result | Approved by |
|------|--------|-------------|
| B searchable Category owner DEV QA | PASS | owner |
| D AI Review thumbnail bg owner DEV QA | PASS | owner |
| C-SHARED owner DEV QA | PASS | owner |
| Optional Windows updater smoke | not performed | — |
| Optional Mac DMG / Open Anyway / Settings A1 | not performed | — |

**No required owner QA remains** for Signoff. Optional smoke may precede publish but is not a Signoff gate.

---

## Human Approvals Obtained

| Approval | Status | Date | Notes |
|----------|--------|------|-------|
| A2 / paid Apple Program decline | obtained | 2026-08-15 | ADR-FP-136 |
| C-SHARED production backend | obtained | 2026-08-15 | Allowlisted Rules/indexes/Functions |
| Design / UX (B, D, C-SHARED) | obtained | 2026-08-15 | Owner QA PASS |
| Studio 1.0.6 release dispatch | **not obtained** | — | Requires phrase below |
| Studio 1.0.6 publish | **not obtained** | — | Draft + smoke; not authorized by dispatch phrase |
| Firebase / App Hosting / DNS / Portal / secrets | N/A | — | Not in this Signoff |

---

## Risks & Known Issues (accepted notes — non-blocking)

| # | Item | Severity | Why non-blocking |
|---|------|----------|------------------|
| 1 | Mac automatic update **install** unsupported under ad-hoc | Medium ops | Documented; manual install path supported; A1 fallback intact |
| 2 | A2 declined indefinitely | Constraint | Explicit owner decision; Windows path intact |
| 3 | Firestore Rules emulator not run (no Java) | Low | Contract/Rules coverage elsewhere; prior C-SHARED note |
| 4 | CI Mac fail-closed copy still says “A2 gated” | Low (messaging) | Behavior correct (`internal-unsigned` required); no code fix during Signoff |

---

## Deferred Items (Roadmap)

- Optional future: Mac-only proactive “manual download” Settings UX (separate plan)
- Optional future: Developer ID / Apple Program **only** after new explicit owner decision
- Optional CI copy reword (“declined” vs “until A2 gated”) — messaging debt
- Optional owner Windows/Mac smoke before publish

---

## Concise 1.0.6 release summary (draft — do not publish yet)

**Fresh Prints Studio 1.0.6**

- Searchable Categories when editing designs and reviewing AI suggestions  
- Shared Staff Gang Sheets (Internal Sheets) for studio-internal print workflow, with Portal kept separate from staff sheets  
- AI Review queue thumbnails stay in sync with the selected artwork background  
- Clearer updater messages when an update cannot be installed  

**Mac:** Install from the DMG (Open Anyway if Gatekeeper prompts). Automatic update **installation** is not supported for this ad-hoc Mac build; if Settings downloads an update that cannot install, install the latest version manually.  
**Windows:** Automatic updates continue to work as before.

---

## Owner release authorization phrase

Exact phrase required before any Studio 1.0.6 Actions dispatch:

```text
AUTHORIZE STUDIO 1.0.6 RELEASE DISPATCH: STABLE INTERNAL-UNSIGNED FROM PRODUCTION 9f945f3
```

**Authorizes only:** Studio release workflow dispatch / build for **1.0.6** from production SHA `9f945f3`, `release_type: stable`, `distribution_mode: internal-unsigned`, producing a **draft** release per existing workflow.

**Does not authorize:** Firebase deploy, App Hosting, DNS, Portal deploy, secrets, A2 / Apple enrollment, notarization, unrelated production mutations, or final GitHub release **publish** (publish remains after draft + existing Windows/Mac artifact smoke gates).

---

## Open Blockers

- [x] None for Signoff approval  
- [ ] Owner release authorization phrase (above) — required before dispatch  
- [ ] Draft smoke / publish — after successful draft build (existing release process)

---

## Verdict

**approved_with_notes**

Rationale: All required product workstreams and Test gates are satisfied; A2 is intentionally declined; accepted limitations are documented and do not block an `internal-unsigned` 1.0.6 draft release from production `9f945f3`. Release **dispatch/publish remain owner-gated**.

---

## Workflow Complete

- [x] `.cursor/workflow/state.md` updated  
- [x] `ROADMAP.md` updated  
- [x] `references/project-chatgpt-handoff/CURRENT-STATE.md` updated  
- [x] `references/project-chatgpt-handoff/13-recent-completed-work.md` updated  
- [x] ADR-FP-136 reflected in handoff decisions  

**Recommended next action for user:** Commit workflow/docs to `development` (docs-only), then reply with the authorization phrase above when ready for dispatch. **STOP before dispatch until that phrase.**
