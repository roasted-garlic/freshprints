# Implementation Review: Studio 1.0.6 — C-SHARED Shared Staff Gang Sheets

| Field | Value |
|-------|-------|
| Date | 2026-08-15 |
| Reviewer | Implementation Review |
| Plan | `docs/workflow/plans/2026-08-14-studio-mac-autoupdate-signing-and-searchable-category-picker-plan.md` (C-SHARED) |
| Formal Review | `docs/workflow/reviews/2026-08-15-studio-1.0.6-workstream-c-shared-staff-gang-sheets-plan-review.md` (`approved_with_changes`) |
| Verdict | **approved_with_notes** |

---

## Summary

C-SHARED corrective implementation matches Formal Review bindings: shared Staff Gang Sheets (no assignee), `studio_internal`-only eligibility, Studio Add to Show / Gang Sheet destinations, one Staff Add Request control, timer/countdown hidden for Staff, transactional create/complete uniqueness via trusted callables, Rules/index updates, and docs (DATA_MODEL + ADR-FP-135).

---

## Binding compliance

| Binding | Status |
|---------|--------|
| Strict `studio_internal` only; no `isInternal` inference | pass |
| Add Request placeholder preserved + filter fix | pass |
| One Staff Add Request (header only) | pass |
| Shared uniqueness on create (Admin TX callable) | pass — `createInitialStaffGangSheet` |
| Completion uniqueness / idempotency | pass — narrowed `completeStaffGangSheetAndOpenNext` |
| Callable auth: any staff complete | pass |
| Rules: shared manage; create owner/admin; no source conversion | pass |
| permissionService shared manage | pass |
| Index: remove assignee composite; add `source + productionStatus` | pass (not deployed) |
| Hide Staff timer + countdown | pass |
| Studio modal Shows \| Staff tabs; Portal untouched | pass |
| DEV legacy: tolerate assignee; fail closed on multi-active | pass |

---

## Notes

1. **Create path:** Client Firestore TX cannot `get(Query)` in this SDK — uniqueness is enforced by new callable `createInitialStaffGangSheet` (owner/admin). Studio service wraps it. Requires **Functions** deploy with Rules/indexes.
2. **Rules emulator tests** were not executed (emulator host/port not available in this session). Recorded honest fail-to-run; must run under `firebase emulators:exec` before/with DEV QA.
3. **DEV fixture cleanup** may still be required if multiple active assigned-lane Staff docs exist — create/complete fail closed until resolved (owner-authorized; not mutated in Implement).

---

## Verification run (this session)

| Check | Result |
|-------|--------|
| Focused `npx tsx --test` (shared/permissions/UI/callable/Portal contracts) | **pass** (28 tests) |
| Studio `tsc --noEmit` | **pass** |
| Functions `tsc --noEmit` | **pass** |
| ESLint touched files | **pass** |
| `git diff --check` (touched paths) | **pass** (no whitespace errors reported) |
| Firestore Rules unit tests | **not run** — emulator not configured |

---

## Deploy checkpoint (owner authorize — do not deploy in this step)

Required for DEV after this Implement:

- `firestore.rules`
- `firestore.indexes.json` (`source` + `productionStatus`; remove assignee composite)
- Functions: `createInitialStaffGangSheet`, `completeStaffGangSheetAndOpenNext` (and existing Portal/Recently Requested functions remain as previously deployed)

---

## Next

Owner-authorized DEV redeploy → owner DEV QA checklist → then FreshForge Test only after C PASS.
