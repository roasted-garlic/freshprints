# Implementation Review: Studio 1.0.4 macOS release support

| Field | Value |
|-------|-------|
| Date | 2026-08-12 |
| Reviewer | Implementation Review |
| Plan | `docs/workflow/plans/2026-08-12-studio-1.0.4-macos-release-support-plan.md` |
| Formal Review | `approved_with_changes` |
| Branch | `release/studio-1.0.4-macos-support` |
| Base | `662b5ef7fde11cd2795201e2f14275cc15e74d55` |
| Verdict | **approved_with_notes** |

---

## Summary

Implementation matches the reviewed Plan: Mac **arm64** DMG+ZIP, shared env fail-closed script, dual CI jobs + finalize (no concurrent `--publish always`), sharp `asarUnpack` + packaged sharp verifier, Windows path preserved, draft `369361779` not mutated. Local automated gates and Windows package regression passed. **Mac CI package on `macos-latest` must still be proven via workflow_dispatch on this branch (prerelease) before merge confidence is complete.**

---

## Binding Formal Review checks

| Requirement | Status |
|-------------|--------|
| arm64-only | pass |
| unsigned internal Mac | pass (`identity: null`, Mac rejects stable `signed`) |
| DMG + ZIP + `latest-mac.yml` | pass (workflow + builder) |
| Windows exe + blockmap + `latest.yml` | pass (local package regression) |
| Shared env fail-closed | pass (`write-studio-release-env.mjs`) |
| Finalize requires both platforms + same SHA | pass |
| No draft 369361779 mutation in source | pass (finalize only matches same-SHA tagged drafts) |
| Menu redesign avoided | pass (left `Menu.setApplicationMenu(null)`; smoke notes Gatekeeper/quit) |
| No Firebase/backend changes | pass |

---

## Local verification

| Check | Result |
|-------|--------|
| Unit/workflow tests | **46 pass** |
| Studio `tsc --noEmit` | **pass** |
| Lint | **pass** |
| `git diff --check` | **pass** |
| Windows electron-builder `--win --x64 --publish never` | **pass** — Setup.exe + blockmap + `latest.yml` |

---

## Notes / follow-ups

1. Dispatch prerelease `studio-release.yml` from this branch to prove Mac arm64 package + sharp verifier on `macos-latest`.
2. After production merge, rebuild **stable** `internal-unsigned` from new tip; supersede Windows-only draft `369361779`.
3. Publication still paused pending dual smoke.

---

## Verdict

**approved_with_notes** — source ready for protected PR; Mac runner proof via CI dispatch required as note, not a Plan scope miss.
