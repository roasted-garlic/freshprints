# Implementation Review: Studio 1.0.4 macOS x64 + arm64 release support

| Field | Value |
|-------|-------|
| Date | 2026-08-12 |
| Reviewer | Implementation Review |
| Plan | `docs/workflow/plans/2026-08-12-studio-1.0.4-macos-release-support-plan.md` |
| Formal Review | `approved_with_changes` |
| Owner amendment | Mac **x64 + arm64**; unsigned-internal; **no GitHub Release mutation** before production merge |
| Branch | `release/studio-1.0.4-macos-support` |
| Head SHA | `adf5eebaa70f080d0266129c3e90d8488996b7ab` |
| Base production SHA | `662b5ef7fde11cd2795201e2f14275cc15e74d55` |
| PR | https://github.com/roasted-garlic/freshprints/pull/64 (**open**, unmerged) |
| Validation CI | https://github.com/roasted-garlic/freshprints/actions/runs/31621714795 — **success** |
| Verdict | **approved_with_notes** |

---

## Summary

Dual native Mac packaging (Intel x64 + Apple Silicon arm64), space-free arch-explicit artifact names, merged `latest-mac.yml`, Windows NSIS download retry, and hard-gated validation-only finalize are implemented and proven on branch CI. **Do not merge until owner authorizes.** Do not publish. Do not run stable release until after merge.

---

## Validation CI results (run `31621714795`)

| Job / check | Result |
|-------------|--------|
| `build-windows` | **PASS** |
| `build-macos` | **PASS** |
| `finalize-release` | **PASS** (`VALIDATION_ONLY_NO_RELEASE_MUTATION=1`) |
| Packaged arm64 Electron arch | **PASS** (`PACKAGED_ELECTRON_ARCH_OK=arm64`) |
| Packaged arm64 sharp | **PASS** (`PACKAGED_SHARP_OK=0.33.5`, `sharp-darwin-arm64.node`) |
| Packaged x64 Electron arch | **PASS** (`PACKAGED_ELECTRON_ARCH_OK=x86_64`) |
| Packaged x64 sharp | **PASS** (`PACKAGED_SHARP_OK=0.33.5`, `sharp-darwin-x64.node`) |
| Big Sur floor | **PASS** (`LSMinimumSystemVersion=11.0` for arm64 and x64) |
| Merged updater metadata | **PASS** (`MERGED_LATEST_MAC version=1.0.4 files=4`) |
| Same SHA | **PASS** (`adf5eebaa70f080d0266129c3e90d8488996b7ab`) |
| Pre-merge release mutation | **NONE** — finalize logged validation-only skip; no draft targeting `adf5eeb…` |

---

## Finalize naming root cause + fix

| Before | After |
|--------|-------|
| `${productName}` → `Fresh Prints-…` (space) | `Fresh-Prints-Windows-…` / `Fresh-Prints-Mac-${arch}-…` |
| `gh release upload` → `Fresh.Prints-…` | Space-free names match `latest.yml` / `latest-mac.yml` |
| Finalize compared wrong basenames | Exact canonical paths + reject spaces / `Fresh.Prints-*` |

---

## Accidental drafts (owner cleanup; not auto-deleted)

| ID | Target | Notes |
|----|--------|-------|
| `369361779` | `662b5ef…` | Windows-only production draft — leave unpublished/superseded |
| `369384310` | `bfbe909…` | Earlier branch finalize (`Fresh.Prints-*`) |
| `369391358` | `dd401e8…` | Earlier branch finalize |

Successful validation run `31621714795` did **not** create or update any of these.

---

## Notes / follow-ups after merge

1. Owner merges PR #64 into `production`.
2. Stable dual-platform rebuild from **new** production tip (`internal-unsigned`).
3. Real Windows + Mac x64 (Big Sur) + Mac arm64 smoke.
4. Separate publish authorization.
5. Owner decides cleanup of accidental drafts `369384310` / `369391358`.

---

## Verdict

**approved_with_notes** — ready for owner production merge checkpoint. Agent **STOP** before merge.
