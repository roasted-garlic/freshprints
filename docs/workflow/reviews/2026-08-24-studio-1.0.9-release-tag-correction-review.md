# Review: Studio 1.0.9 release tag correction

| Field | Value |
|-------|-------|
| Date | 2026-08-24 |
| Reviewer | Review Agent |
| Plan | docs/workflow/plans/2026-08-24-studio-1.0.9-release-tag-correction-plan.md |
| Verdict | **approved_with_changes** |

---

## Summary

Investigation is complete and evidence-backed. The `untagged-*` tag was assigned during `studio-release.yml` finalize (run **32754684436**); publish helper correctly set Latest + final copy but **never retagged** to `v1.0.9`. Remote `v1.0.9` is absent; in-place PATCH of release **375869566** is the safest fix and preserves assets, Latest, and updater metadata. Phase 1 must remain read-only; retag + workflow hardening belong in Phase 2 after owner phrase.

---

## Checklist

| Area | Status | Notes |
|------|--------|-------|
| Scope clear and bounded | pass | Phase 1 investigation + plan only; mutations gated |
| Architecture alignment | pass | GitHub API at existing publish boundary; no product layers |
| Security impact addressed | pass | Owner-gated PATCH; no secrets; fail-closed preflight |
| Data model impact addressed | pass | None |
| Backend impact addressed | pass | None |
| Test strategy adequate | pass | Phase 2 unit tests + manual API/updater checks listed |
| Human checkpoints identified | pass | Retag phrase + optional orphan delete + implement phrase |
| Roadmap alignment | pass | Post-promote hygiene; Phase 9 PARKED |
| Documentation plan | pass | DEPLOYMENT.md + signoff record in Phase 2 |
| No silent scope expansion | pass | No rebuild, no v1.0.8 touch |

---

## Architecture Review

**Findings:**

- Tag assignment is split across **finalize** (draft create) and **publish** (draft → published). Missing contract: published stable releases must end with `tag_name=v{version}`.
- Publish helper is the correct place to normalize tag on stable publish (single owner checkpoint).

**Required changes:**

- [ ] None for Phase 1

---

## Security Review

**Findings:**

- Retag is a destructive GitHub/git operation — correctly gated behind explicit owner phrase.
- PATCH by numeric release id reduces risk of touching wrong release.

**Required changes:**

- [ ] None

**Human approval needed before production:**

- [x] `APPROVE STUDIO RETAG: 1.0.9 RELEASE 375869566 TO v1.0.9` before any PATCH
- [x] `APPROVE IMPLEMENT: studio-1.0.9-release-tag-correction` before workflow script changes

---

## Testing Review

**Findings:**

- Phase 1 acceptance criteria met (10/10) with read-only evidence.
- Phase 2 should add unit test asserting publish PATCH includes `tag_name: "v1.0.9"` and verify rejects `untagged-*`.

**Required changes:**

1. Add publish-helper test for expected `tag_name` in PATCH payload (Phase 2).

---

## Required changes (approved_with_changes)

1. Phase 2 implement must add automated test coverage for `tag_name` in publish PATCH (listed above).
2. Record post-retag API snapshot in a workflow review doc before signoff (Phase 2).

---

## Blockers

None for Phase 1. **Phase 2 mutation blocked** until owner retag phrase.

---

## Verdict rationale

**approved_with_changes** — Root cause, inventory, safest correction path, updater impact, and rollback are documented with file/line references and API proof. In-place retag preserves release **375869566** without rebuild. Minor test/doc additions required in Phase 2 implement.

---

## Next step

1. **STOP** — no GitHub mutation in this pass.
2. Owner sends `APPROVE STUDIO RETAG: 1.0.9 RELEASE 375869566 TO v1.0.9` when ready for Phase 2 retag.
3. Owner sends `APPROVE IMPLEMENT: studio-1.0.9-release-tag-correction` for publish/finalize hardening + tests.
