# Evidence gate addendum: BASE vs HEAD transparency-corrective regression diff

| Field | Value |
|-------|-------|
| Date | 2026-08-13 |
| Managed goal | `studio-1.0.4-ai-processing-preview-cleanup-corrective` |
| Status | **COMPLETE comparison — STOP (locus still not proven for smoke failure)** |
| Classification | **C — TRANSPARENCY CORRECTIVE DISPROVEN** as cause of derivative failure (local controlled + source-diff). Packaged/runtime locus for the original smoke failure remains open (**D** still relevant for next evidence). |

---

## Required return summary

| # | Item | Result |
|---|------|--------|
| 1 | BASE SHA | `4ac77141f0f12845e1ca11b742c5242488496b70` |
| 2 | HEAD SHA | `975f6400262a86600c4662f39480c6f55e20b0c1` |
| 3 | Executable files changed (Studio import path) | See below |
| 4 | `package.json` change | **Only** `test:rules` script adds `designArchiveExpressionBudget.rules.test.ts` — **no** Sharp/Electron/dependency version change |
| 5 | `pngValidator` ordering | Transparency gate **inserted** after `parsePngMetadata`, **before** trim/upscale; all later steps unchanged |
| 6 | Validation-output semantics | On PASS: same return shape path; only new fail path `BACKGROUND_NOT_TRANSPARENT` / check failure |
| 7 | `correctedImportBytes` / cache | **Unchanged** code; still caches only when trim/upscale corrected; transparency does not write/consume cache |
| 8 | Sharp usage differences | HEAD adds **extra** Sharp use during validation (measure transparency). Derivative Sharp path **unchanged** |
| 9 | BASE reproduction | Validation n/a-sim; trim/upscale/derivative gen + WebP upload-assert **PASS** |
| 10 | HEAD reproduction | Transparency PASS; trim/upscale/derivative gen + WebP upload-assert **PASS** (identical sizes) |
| 11 | First diverging pipeline stage | **NONE** through derivative byte generation + client upload-byte validation |
| 12 | Did transparency corrective cause the regression? | **No** (not supported by source diff or controlled local repro) |
| 13 | Supporting evidence | Diff + probe JSON below |
| 14 | Test coverage gap | Yes — transparency tests stop at validation; no validate→derivatives→upload/persist regression test |
| 15 | Packaged runtime evidence still required? | **Yes** for pinning the smoke failure locus (Windows draft / local Electron full import) |
| 16 | Read-only prod original probe still required? | **Optional next** — useful to rule out fixture-byte-specific encode failure; **not** justified as “transparency regression proof” anymore |
| 17 | Recommended next action | Collect packaged/local Studio `derivativeError` **or** authorize read-only prod original probe **or** run full local Studio import against DEV with DevTools logging — then Implement |

Current production tip `c6e9235` remains Portal-only after `975f640` for Studio runtime.

---

## Step 1 — Exact commit range

Commits `4ac77141..975f640`:

1. `b488a43` — `fix(studio): import transparency gate + archive Rules fast path (1.0.4)`
2. `975f640` — merge PR #67

### Executable / runtime Studio import changes

| Path | Role |
|------|------|
| `apps/studio/electron/ipc/import/pngValidator.ts` | Insert transparency gate before trim |
| `apps/studio/electron/services/import/importMeaningfulTransparencyCheck.ts` | **New** gate wrapper |
| `apps/studio/electron/ipc/import/mapPngValidationFailureToRejection.ts` | Propagate `reasonCode` |
| `packages/shared/src/utils/meaningfulTransparencyMeasurement.ts` | **New** shared Sharp measurement |
| `packages/shared/src/types/import/batchImport.types.ts` | Add `BACKGROUND_NOT_TRANSPARENT` reason |

### Explicitly **unchanged** (verified `git diff --name-only`)

- `enrichReadResultWithDerivatives.ts`
- `derivativeGenerationService.ts`
- `importOrchestrationService.ts`
- `importDerivativeService.ts`
- `importBatchOrchestrationService.ts`
- `loadSharpModule.ts`
- `correctedImportBytesCache.ts`
- `readSelectedPngFileBytes.ts`

### Other range changes (not Studio derivative pipeline)

| Path | Note |
|------|------|
| `firestore.rules` | Soft-archive expression-budget fast path (Defect B) — not import derivatives |
| `functions/src/lib/customerUploadProcessing.ts` | Refactor to shared measurement — customer uploads, not Studio import derivatives |
| `package.json` | `test:rules` list only |
| Tests/docs/`.gitignore` | Non-runtime for packaged Studio import derivatives |

---

## Step 4 — Statement-level `pngValidator` ordering

### BASE (`4ac77141`)

1. extension / stat / size checks
2. `readFile` → `fileBuffer`
3. `parsePngMetadata(fileBuffer)`
4. `trimImportImageIfNeeded(fileBuffer)`
5. print-size reject assessment
6. `upscaleImportImageIfNeeded(trim…)`
7. optional `cacheCorrectedImportBytes`
8. post-upscale assessment + warnings + return `ValidateSelectedPngFileResult`

### HEAD (`975f640`)

1–3 identical
**3b. NEW:** `evaluateImportPngMeaningfulTransparency(fileBuffer)` → throw on fail
4–8 identical to BASE

No reordering of trim/upscale/cache/return relative to each other — only an **additive early reject** before trim.

---

## Steps 2–3 — Semantics + controlled reproduction

Probe: `apps/studio/scripts/probe-transparency-derivative-regression.mts`
Synthetic large transparent PNG (~0.86 MB compressed; trim/upscale exercised).

| Check | BASE (no transparency) | HEAD (with transparency) |
|-------|------------------------|---------------------------|
| Transparency | n/a | PASS |
| Source `Buffer` same ref after gate | n/a | **true** |
| Source byteLength unchanged | true | **true** |
| Trim / upscale outputs | identical | identical |
| Derivative generation | PASS | PASS |
| `assertValidDerivativeWebpUploadBytes` | PASS | PASS |
| First divergence | — | **NONE** |

Disproved for this controlled path:

- shared Buffer consumption / mutation by transparency measurement
- corrected-byte cache interference (not involved when probing generation input from upscale bytes)
- IPC serialization (not in this Node probe; prior Electron IPC Uint8Array probe also PASS)
- partial validation success that still creates designs (opaque would **fail** validation before create — opposite of smoke)
- derivative service code regression in this commit range (downstream files unchanged)

Not exercised here (still need later evidence for smoke locus):

- Firebase Storage derivative upload
- Firestore `markDesignDerivativesComplete`
- background AI enqueue
- packaged Electron ASAR Sharp resolution on draft `369614747`
- exact production fixture PNG bytes

---

## Step 5 — `package.json` / Sharp

Exact change: append one rules unit-test path to `test:rules` script.

**Cannot** affect Electron Sharp resolution, ASAR unpack, dependency pruning, or packaged native binaries.

Local Sharp encode previously PASS; this range does not change Sharp packaging.

---

## Step 6 — Test coverage gap

Transparency corrective tests cover:

- opaque / alpha-but-opaque reject
- transparent accept
- rejection mapping

They do **not** cover:

validation → corrected bytes → derivative generation → Storage upload → path persistence → auto AI enqueue

**Coverage gap: confirmed.**

---

## Decision classification

### **C — TRANSPARENCY CORRECTIVE DISPROVEN**

as the cause of “originals succeed / derivatives missing” for imports that **pass** validation.

Rationale:

1. Derivative/orchestration/IPC/cache modules are **byte-identical** BASE→HEAD.
2. Ordering change is only an early fail-closed gate; PASS path continues into the same trim/upscale/cache contract.
3. Controlled HEAD-with-gate vs BASE-without-gate both complete derivative generation + upload-byte validation identically.
4. Smoke fixtures **exist as designs** ⇒ they passed validation (including transparency on HEAD) ⇒ failure is **after** the corrective’s inserted statement.

### Remaining for smoke locus (not transparency causation)

Prior timestamp analysis still points at:

- some files: missing derivatives on read (generation)
- most files: post-create updates ⇒ upload/persist/rollback

Those stages are **outside** the transparency diff. Next evidence should target them (packaged/local full import error string, and/or read-only prod original probe for encode-on-real-bytes).

**D** remains available if packaged Windows-only behavior appears later — but that would **not** revive transparency as the root cause without new contradictory evidence.

---

## Recommended next action

1. Prefer owner `DERIVATIVE LOCUS EVIDENCE: …` from local-dev or packaged Windows import UI/DevTools.
2. Else `AUTHORIZE READ-ONLY PROD ORIGINAL DERIVATIVE LOCUS PROBE` (fixture-byte encode check only).
3. Do **not** implement a transparency-gate rollback as the Studio 1.0.4 derivative fix — evidence does not support it.
4. Continue **STOP** on Implement until locus is proven.

---

## Forbidden actions (honored)

No implementation, prod mutation, fixture mutation, Start AI, deploy, PR, draft mutation.
