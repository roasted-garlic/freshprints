# Manual smoke: Studio Windows 1.0.4 (draft `369614747`)

| Field | Value |
|-------|-------|
| Date | 2026-08-13 |
| Platform | **Windows** |
| Draft release ID | **`369614747`** |
| Build SHA | `975f6400262a86600c4662f39480c6f55e20b0c1` |
| Environment | Production `fresh-prints-prod` + Algolia search-only `Z1FVCM5QUX` / `portal_catalog_ready_prod` |
| Why automated tests are insufficient | Packaged Electron install, auth, and production workflows require a real Windows workstation |

## Exact artifact (must match)

| Item | Value |
|------|-------|
| File | `Fresh-Prints-Windows-1.0.4-Setup.exe` |
| Size | `112000263` bytes |
| SHA-256 | `85748c54bdb6d6dbfa16cb1bf57cdbfc592c69e29f32f6094cf6381ba12750e8` |
| Download | Draft `369614747` only — https://github.com/roasted-garlic/freshprints/releases/tag/untagged-d31b85ddaa62bf6f9b78 |

Do **not** smoke older drafts. Verify SHA-256 before install.

## Prerequisites

- Uninstall / replace prior Studio builds so this EXE is what runs
- Confirm app talks to **production** after login (not `fresh-prints-dev`)

## Steps

1. Install from the EXE above → **Expected:** install completes; app opens as **1.0.4**
2. Login to production → **Expected:** auth succeeds; no DEV project markers
3. Design Library → **Expected:** library loads; designs visible
4. Search / filter → **Expected:** search and filters behave; no hard crash
5. Imports → **Expected:** import UI opens and is usable
6. Transparent-image import → **Expected:** accepted
7. Opaque-image import → **Expected:** rejected with clear error
8. Folder selection → **Expected:** folder pick works for import path
9. ZIP import → **Expected:** ZIP import path works or fails safely with clear error
10. AI Review → **Expected:** review queue / workflow reachable for eligible items
11. Approve / reject / archive → **Expected:** intended actions succeed without permission-denied on allowed roles
12. Customer Uploads → **Expected:** list/detail usable
13. Print Requests → **Expected:** list/detail usable
14. Show Queue → **Expected:** queue usable for authorized role
15. Export / download → **Expected:** export or download completes for a known path
16. Updater / update check → **Expected:** check does not crash (unsigned/internal may show no update)
17. Quit / relaunch → **Expected:** clean quit; relaunch restores session/UI

## Pass criteria

- [ ] Artifact SHA-256 matched before install
- [ ] All steps above PASS on this draft EXE
- [ ] No DEV Firebase/Algolia behavior
- [ ] Prior draft results not used

### Please reply with

- `WINDOWS SMOKE 1.0.4: PASS`
- `WINDOWS SMOKE 1.0.4: FAIL: [description]`
- `WINDOWS SMOKE 1.0.4: PASS WITH NOTES: [notes]`
