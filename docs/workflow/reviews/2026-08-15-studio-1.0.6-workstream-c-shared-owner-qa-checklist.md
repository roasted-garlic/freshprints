# Owner DEV QA — Studio 1.0.6 C-SHARED Shared Internal Gang Sheets

| Field | Value |
|-------|-------|
| Date | 2026-08-15 |
| Goal | `studio-mac-autoupdate-signing-and-searchable-category-picker` |
| Target | Studio **1.0.6** |
| Prerequisites | Owner-authorized DEV redeploy of Rules + indexes + Functions (`createInitialStaffGangSheet`, `completeStaffGangSheetAndOpenNext`) |

## Before QA

- [ ] DEV Rules deployed
- [ ] DEV indexes deployed (`source` + `productionStatus`)
- [ ] DEV Functions deployed (create + complete callables)
- [ ] If create fails with “another active Internal Gang Sheet”, owner cleanup of legacy multi-lane opens (do not auto-merge)

## Checklist

### Shared create / uniqueness

- [ ] Create Internal Gang Sheet is hidden while any sheet is open/full/printing; Mark Complete opens the next cycle
- [ ] When no active sheet exists (empty or history-only), Create opens max(existing)+1 (not always #1)
- [ ] Add to Internal Gangsheet lists a picker when more than one active sheet exists (recovery / legacy)

### Eligibility / Add Request

- [ ] Internal Add Request list shows `studio_internal` requests with allocatable quantity
- [ ] Placeholder “Choose a request” remains
- [ ] `studio_customer` / `portal_customer` / missing-origin requests do not appear
- [ ] Exactly **one** Add Request button on Internal detail (Attached print requests header); none duplicated elsewhere
- [ ] Completed Internal sheet cannot add allocations

### Studio Add to Show / Internal Sheet

- [ ] Sidebar has **Internal Sheets** separate from **Show Queue** (no Internal tab on Show Queue)
- [ ] Print Requests has separate **Add to Show** and **Add to Internal Sheet** buttons
- [ ] Add to Show opens calendar modal only (no Internal tab)
- [ ] Add to Internal Sheet opens Internal Sheet modal only (no calendar)
- [ ] Internal modal: current open shared sheet; no calendar/capacity/split; no auto-create
- [ ] Empty Internal modal: “No open Internal Gang Sheet”
- [ ] Internal / `studio_internal` requests can be attached from Internal Sheets Add Request and from Print Requests → Add to Internal Sheet
- [ ] Non-internal request on Internal modal: clear deny copy

### Timer / countdown / Whatnot

- [ ] Internal detail has no production timer and no countdown/time-until-show UI
- [ ] Whatnot show timer, schedule, capacity, export unchanged

### Completion

- [ ] Mark Complete enabled when the sheet has at least one attached print request (Generate not required)
- [ ] Mark Complete opens a confirm modal; confirming creates shared #N+1 (no assignee)
- [ ] Empty sheet (no allocations): Mark Complete stays disabled
- [ ] Show Queue **Mark finished** enabled when printing and the show has ≥1 allocation (Generate not required); confirm modal before finish
- [ ] Double-click / retry does not create duplicate open sheets
- [ ] History retains completed sheets

### Portal / Recently Requested

- [ ] Portal has no Internal destination / still “Add to Show” style copy
- [ ] Portal cannot queue onto Internal sheet IDs
- [ ] Internal allocations do not bump Recently Requested

## Result

**PASS** — 2026-08-15 owner confirmation: Internal Sheets / Print Requests flows working as expected after Queued↔Working sync + remove UX fixes (and prior C-SHARED QA fixes in this session).

Reply with: `PASS` | `FAIL: …` | `PASS WITH NOTES: …`
