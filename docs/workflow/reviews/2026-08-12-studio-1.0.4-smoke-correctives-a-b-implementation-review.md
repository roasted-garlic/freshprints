# Implementation Review: Studio 1.0.4 smoke correctives A+B

| Field | Value |
|-------|-------|
| Date | 2026-08-12 |
| Reviewer | Implementation Review |
| Plan | `docs/workflow/plans/2026-08-12-studio-1.0.4-smoke-correctives-a-b-plan.md` |
| Formal Review | `approved_with_changes` |
| Branch | `hotfix/studio-1.0.4-smoke-correctives-a-b` |
| Starting SHA | `4ac77141f0f12845e1ca11b742c5242488496b70` |
| Verdict | **approved** |

---

## Summary

Implemented Defect A (Studio import meaningful-transparency gate via shared measurement) and Defect B (narrow `designArchiveStatusOnlyUpdate` Firestore Rules fast path). Local automated tests pass, including customer-upload processing regression and Rules emulator archive matrix. **No DEV/prod deploy performed.**

---

## Defect A

- Gate in `validatePngFile` on **source bytes** before trim/upscale (covers single/folder/ZIP/multi).
- Shared `measureMeaningfulTransparency` + existing `assessMeaningfulTransparency` thresholds (α&lt;250, 0.5%, 1% trim).
- Staff message: catalog artwork requires transparent background.
- Rejection reason `BACKGROUND_NOT_TRANSPARENT`.
- Functions `customerUploadProcessing` uses shared measurement; existing processing tests **23/23 PASS**.

## Defect B

- `designArchiveStatusOnlyUpdate()` with six-field allowlist, `previousStatus` match, UID/timestamp/purge guards.
- Placed before approval fast path; approval cheap-fails when target status is `archived`.
- Emulator: enrichment-heavy rejected→archived ALLOW (owner/helper); pre-corrective DENY without fast path; deny matrix green; approve suites green.

## Automated results

| Check | Result |
|-------|--------|
| Shared predicate tests | PASS (7) |
| measureMeaningfulTransparency | PASS (3) |
| customerUploadProcessing | PASS (23) |
| pngValidator transparency + trim/upscale | PASS (11) |
| Archive Rules suite | PASS (12) |
| Approval expression-budget + sequential | PASS |
| Studio `tsc --noEmit` | PASS (after generate-packaged-build-config) |
| Functions `tsc` build | PASS |
| `npm run lint` | PASS |
| `git diff --check` | PASS |

## Binding Formal Review compliance

1. Source `hasAlpha` + sampled pixels / trim probe — yes  
2. Archive cheap-fail approval for `archived` + six-field allowlist — yes  
3. Emulator deny-before (legacy rules strip) + allow-after — yes  
4. Smoke FAIL doc for draft `369535832` — yes  
5. No publish / draft mutate / prod deploy — yes  

## DEV deploy notes

- **Firestore Rules:** required on `fresh-prints-dev` before owner DEV QA for Defect B.  
- **Functions deploy:** **not required** for DEV verification of A+B. Studio Electron ships the import gate in the Studio binary; Functions change is a behavior-preserving shared-measurement refactor already covered by local Functions tests. DEV QA for A uses local/dev Studio against DEV Firebase; B needs Rules deploy only.

## Not done / next gate

- DEV Rules deploy (await owner phrase)
- Owner DEV QA
- PR push / ChatGPT merge
- Production Rules / new draft rebuild / re-smoke / publish

---

## Verdict Rationale

Diff matches approved plan + binding review changes; focused automated evidence is green; scope stayed narrow.
