# Dirty-tree classification + commit record — C-SHARED signed-off tip

| Field | Value |
|-------|-------|
| Date | 2026-08-15 |
| Final commit | `0451bc4bf42c32d3be4ffeff9731d0aef0816bd5` |
| Message | `chore(studio): capture signed-off 1.0.6 C-SHARED state` |
| Branch | `feature/studio-1.0.6-mac-signing-and-searchable-category` |
| Working tree after commit | **clean** |
| Excluded / unrelated | **none** |

## Classification (all dirty paths)

### 1 — Signed-off C-SHARED correctives
- Studio: permissions, AddToShowModal, Print Requests (pages/hooks/service), UpcomingShows/Internal Sheets pages+services+utils+routes+sidebar+CSS, export gang sheet persist
- Backend: `firestore.rules`, create/complete Staff callables + tests, `syncPrintRequestQueueTab`, `printRequestQueueTab` lib, queueTab trigger refactor, `portalWorkingPrintRequest`, `index.ts`
- Shared: `staffGangSheet` utils/types/tests
- Rules unit test updates
- `docs/architecture/DATA_MODEL.md`, owner QA checklist updates

### 2 — Already-approved A1/B/D / 1.0.6 release-branch UX
- `AiReviewWorkspace.tsx`, `DesignFormFields.tsx` — Halftone → lightBlack artwork background (Decision Log pre-QA UI tweak)

### 3 — Workflow/docs for this completed work
- `.cursor/workflow/state.md`, `ROADMAP.md`, `DECISIONS.md`
- Signoff, test report, production promotion preflight

### 4 — Unrelated / accidental / generated / local-only
- **None** — no `dist*`, secrets, `.env`, or unexplained paths

## Feature → development
- PR: https://github.com/roasted-garlic/freshprints/pull/76
- State: OPEN, MERGEABLE
- Merge: **not completed in-agent** (Cursor hook blocked `gh pr merge`); owner must merge via GitHub UI or authorized merge path

## Production PR
- Deferred until #76 lands on `origin/development`
- Diff preview for tip `0451bc4` vs `origin/production` is the expected production PR content (see agent return / preflight)
