# STOP: Packaged DEV diagnostic Studio build — awaiting UI Imports evidence

| Field | Value |
|-------|-------|
| Date | 2026-08-13 |
| Managed goal | `studio-1.0.4-ai-processing-preview-cleanup-corrective` |
| Baseline HEAD | `c6e9235614b6816a98a71f998b47bd7fe18c371f` |
| Diagnostic build contents | Worktree on that HEAD **plus uncommitted diagnostic instrumentation** (baked `PACKAGED_DERIVATIVE_LOCUS_DIAG=true`, `VITE_FP_DERIVATIVE_LOCUS_DIAG=1`) |
| Status | **STOPPED — package built & launched; UI Imports batch requires human** |
| Draft `369614747` | **Untouched** |
| Publish | **None** (`--publish never`) |

---

## Required return so far (1–27)

### 1. Diagnostic build source SHA
Baseline: `c6e9235614b6816a98a71f998b47bd7fe18c371f`
Packaged bytes include local diagnostic instrumentation not yet committed.

### 2. Diagnostic artifact path/name
- Installer: `apps/studio/release/1.0.4-deriv-locus-diag.dev/Fresh-Prints-Windows-1.0.4-deriv-locus-diag.dev-Setup.exe`
- Unpacked (launched): `apps/studio/release/1.0.4-deriv-locus-diag.dev/win-unpacked/Fresh Prints.exe`
- Version suffix: `1.0.4-deriv-locus-diag.dev` (distinguishable from release `1.0.4`)

### 3. Packaged runtime Firebase project/bucket verification
- Pre-bake check: `.env.local` `VITE_FIREBASE_PROJECT_ID=fresh-prints-dev`
- Dist scan: `fresh-prints-dev` present; `fresh-prints-prod` **absent**
- UI fail-closed: `DiagnosticProjectGate` blocks app if diagnostic flag set and project ≠ `fresh-prints-dev`
- Expected UI banner: `DIAGNOSTIC BUILD — Firebase project fresh-prints-dev …`
- **Owner must confirm banner/project before importing**

### 4. OS / platform
Windows x64 (local owner machine)

### 5–20. UI import workflow / stage tables / first failure
**NOT YET CAPTURED** — requires human Imports UI run (login + real file picker). App process was launched.

### 21. Classification
**Incomplete — pending UI batch.** Not P1–P6 yet.

### 22. Proven root cause
**Not established.**

### 23. Platform-scope
**Not established** (packaged UI not yet exercised to failure/pass).

### 24. Primary implementation can proceed?
**NO.**

### 25. Next evidence step
Owner completes Manual Test Checkpoint below; reply with results + confirm diagnostic JSONL under `%USERPROFILE%\.fresh-prints\diagnostics\`.

### 26. Checkpoint path
`docs/workflow/reviews/2026-08-13-studio-1.0.4-packaged-dev-diagnostic-studio-build.md`

### 27. Recommended next FreshForge command (after UI evidence)

```text
CONTINUE WORKFLOW: PACKAGED DEV DIAGNOSTIC UI RESULTS
```

Paste: classification P1–P6, first failing stage, sanitized error, and whether banner showed `fresh-prints-dev`.

---

## What was built (diagnostic-only)

| Item | Detail |
|------|--------|
| Bake flags | `STUDIO_DIAGNOSTIC_BUILD=1` → `PACKAGED_DERIVATIVE_LOCUS_DIAG=true` |
| Renderer | `VITE_FP_DERIVATIVE_LOCUS_DIAG=1` enables stage logs in packaged mode |
| Sink | `~/.fresh-prints/diagnostics/derivative-locus-*.jsonl` (no image bytes / secrets) |
| Updater | Skipped when diagnostic bake is true (avoids GitHub draft/release polling) |
| Publish | `--publish never` |
| Fixtures ready | `apps/studio/tmp/deriv-locus-diag-fixtures/packaged-diag-batch-{1..4}.png` (~11.5 MB, 4500×4500, alpha) |

Optional agent-only dialog bypass (`FP_DIAG_IMPORT_PATHS`) exists for diagnostic builds but was **not** set for this launch so the **real OS file picker** is used.

---

## Manual Test Checkpoint

**Feature / area:** Packaged DEV diagnostic Studio — Imports batch derivative locus
**Why automated tests are insufficient:** Requires staff login + native file dialog + AI Processing visual confirmation
**Environment:** Packaged `1.0.4-deriv-locus-diag.dev` → **fresh-prints-dev only**
**Prerequisites:** Staff account on DEV; fixtures folder above; app already launched (or re-run unpacked exe)

### Steps
1. Confirm green banner shows **fresh-prints-dev** (not production). If blocked/red → **STOP** and report.
   → **Expected:** Diagnostic banner with `fresh-prints-dev`
2. Sign in as staff on DEV.
   → **Expected:** Normal Studio shell
3. Open **Imports** → select **multiple PNG** (or equivalent batch) → choose all 4 files in `apps/studio/tmp/deriv-locus-diag-fixtures\`.
   → **Expected:** Files validate; batch upload runs
4. Wait for batch completion. Note result panel: success / derivative failures / `derivativeError` text.
   → **Expected:** Record exact UI wording
5. Open **AI Processing**. For each new design note: thumb visible? large preview? WAITING FOR AI? auto-start without Start AI?
   → **Expected:** Record per design
6. Leave app open briefly so JSONL flushes; then reply with observations.

### Pass criteria (for evidence, not product pass)
- [ ] Runtime project confirmed `fresh-prints-dev`
- [ ] Real Imports UI + real file picker used
- [ ] ≥4-file batch completed or failed with visible result
- [ ] AI Processing observations recorded
- [ ] Owner notes any `derivativeError` text shown

### Please reply with
- `PACKAGED DIAG P6 PASS: …` — full pipeline + auto AI OK on packaged DEV
- `PACKAGED DIAG FAIL: stage=… error=…` — first failing stage + sanitized error
- Or `PASS WITH NOTES: …` / `FAIL: …` using the Manual Test format

---

## Forbidden (still)

- No primary speculative fix
- No Option B implement yet
- No production writes / fixtures / Start AI on prod
- No draft `369614747` mutation / publish
- No production PR / deploy
