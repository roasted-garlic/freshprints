# Manual Test Checkpoint: Studio 1.0.4 macOS x64 + arm64 (internal ad-hoc)

| Field | Value |
|-------|-------|
| Date | 2026-08-12 |
| Feature / area | Fresh Prints Studio 1.0.4 Mac packages after **ad-hoc signing corrective** |
| Why automated tests are insufficient | Packaged Electron + sharp + Gatekeeper + real Mac UX require physical Macs |
| Environment | Production Firebase `fresh-prints-prod` + Algolia `Z1FVCM5QUX` / `portal_catalog_ready_prod` |
| Distribution | **internal-unsigned** workflow mode = **ad-hoc codesign** (structurally valid). **Not** Developer ID. **Not** notarized. |
| Architectures | **x64** (Intel Big Sur 11.7.11) and **arm64** (Apple Silicon). Do not use Rosetta as the primary M2 path. |

## Do not use failed-smoke Mac builds

- Draft `369431950` Mac DMGs from SHA `d1987b0…` / run `31624159614` **failed** Apple Silicon smoke (`codesign`: missing sealed resources / “damaged”).
- Smoke **only** a **new** dual-platform draft built after the ad-hoc signing corrective merges.
- Do **not** clear quarantine (`xattr`) as the product fix. If the app is still “damaged” after the corrective, stop and report — that is still a packaging defect.

## Gatekeeper / install notes (ad-hoc / unverified)

1. Download the arch-matching `.dmg` from the **new** draft GitHub Release:
   - Intel: `Fresh-Prints-Mac-x64-1.0.4-Installer.dmg`
   - Apple Silicon: `Fresh-Prints-Mac-arm64-1.0.4-Installer.dmg`
2. Open the DMG and drag Fresh Prints to Applications.
3. Expected Gatekeeper friction: **unverified developer** (not “damaged”):
   - Finder → right-click the app → **Open** → confirm Open, **or**
   - System Settings → Privacy & Security → Open Anyway
4. Optional diagnostic (if launch still fails):
   - `codesign -v -vvv --strict --deep "/Applications/Fresh Prints.app"` → **Expected:** exit 0
   - `codesign -d -vvv "/Applications/Fresh Prints.app"` → **Expected:** ad-hoc signature evidence (not Developer ID)
5. Do **not** distribute publicly until a future Developer ID + notarization phase.

## Prerequisites

- Intel Mac on **macOS Big Sur 11.7.11** for x64 smoke
- Apple Silicon Mac for arm64 smoke
- Helper and/or Owner account as needed
- New draft assets for **both** Mac arches (+ Windows if same-SHA rebuild)

### Steps (run on EACH Mac architecture)

1. Install/open Studio from the arch-matching DMG → **Expected:** launches (after Open Anyway if needed); **not** “damaged / Move to Trash”
2. Login → **Expected:** auth succeeds against production
3. Design Library → **Expected:** catalog loads
4. Search → **Expected:** managed search works
5. Tag/category filters → **Expected:** filters apply; Load More behaves correctly when filtered
6. Imports → **Expected:** Imports UI reachable
7. Folder picker → **Expected:** native folder dialog works
8. ZIP import → **Expected:** ZIP selected and processed
9. AI Review → **Expected:** operational for eligible designs
10. Customer Uploads → **Expected:** staff actions work
11. Print Requests → **Expected:** list/detail usable
12. Show Queue → **Expected:** queue usable; Helper cannot open Show Queue Settings; Owner/Admin can
13. Downloads/exports → **Expected:** save dialogs / exports complete
14. Update check → **Expected:** does **not crash**. Mac auto-update may be unsupported on ad-hoc builds until Developer ID — note result; do not fail smoke solely because an update cannot install
15. Quit/relaunch → **Expected:** clean quit and relaunch
16. Normal Mac window behavior → **Expected:** traffic lights / dock activate OK
17. Native image processing (sharp) → **Expected:** derivative / trim / upscale path succeeds

### Pass criteria

- [ ] All steps PASS on **Intel x64** (Big Sur 11.7.11)
- [ ] All steps PASS on **Apple Silicon arm64**
- [ ] No “damaged” dialog on either arch
- [ ] No DEV Firebase/Algolia behavior
- [ ] Gatekeeper friction documented if encountered (unverified path only)
- [ ] Windows smoke also PASS on the **same** dual-platform draft SHA (when that draft exists)

### Please reply with

- `MAC SMOKE 1.0.4: PASS` (both arches)
- `MAC SMOKE 1.0.4: FAIL: …`
- `MAC SMOKE 1.0.4: PASS WITH NOTES: …`
