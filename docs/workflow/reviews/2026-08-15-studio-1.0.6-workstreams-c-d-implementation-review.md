# Implementation Review — Studio 1.0.6 Workstreams C + D

**Date:** 2026-08-15  
**Goal:** `studio-mac-autoupdate-signing-and-searchable-category-picker`  
**Branch:** `feature/studio-1.0.6-mac-signing-and-searchable-category`  
**Plan:** `docs/workflow/plans/2026-08-14-studio-mac-autoupdate-signing-and-searchable-category-picker-plan.md`  
**C+D Review:** `approved_with_changes`

## Overall verdict

**approved_with_notes** for C+D implementation scope.

Does **not** close the managed goal (A2 + owner DEV QA + Test/Signoff remain).

---

## Classification

| ID | Area | Verdict | Notes |
|----|------|---------|-------|
| **C1** | Data model / source | **pass** | `UpcomingShowSource` includes `staff_gang_sheet`; `whatnotShowId` required for Whatnot / omitted for Staff; only `assignedStaffUserId` + `staffGangSheetCycleNumber` added |
| **C2** | Rules / permissions | **pass** | Source-conditional validators; create Staff = owner/admin; helper update own lane only; cannot reassign/convert source; permissionService capabilities added |
| **C3** | Studio Staff UI | **pass** | Show Queue surface tabs Shows \| Staff Gang Sheets; create lane picker; hide Whatnot/capacity chrome; Mark Complete |
| **C4** | Allocation / capacity | **pass** | Omit `maxTotalQuantity`; origin filter in allocate + AddToShowModal; unlimited via existing undefined capacity |
| **C5** | Portal isolation | **pass** | list/queue reject Staff; calendar visibility guard; contract tests |
| **C6** | Recently Requested | **pass** | `onShowAllocationCreated` skips popularity bump when parent `source === staff_gang_sheet` |
| **C7** | Completion / auto-cycle | **pass** | Trusted callable `completeStaffGangSheetAndOpenNext` (required because Rules keep create/assign owner/admin-only) |
| **C8** | Export reuse | **pass** | No new exporter; existing Show Queue ZIP/gang export paths unchanged |
| **D** | AI Review left-rail bg | **pass** | `artworkBackgroundHex` resolved same as workspace; wired into `AiReviewQueueList` → thumbnail |

---

## Binding decision compliance

1. Reuses `upcomingShows` + `showAllocations` — yes  
2. No synthetic Whatnot IDs — yes  
3. Origins allow studio_internal + studio_customer; deny portal_customer — yes  
4. Helper assignment enforced in Rules + callable — yes  
5. Portal never allocatable — yes  
6. Recently Requested skip — yes  
7. Index added only for real open-lane query in complete callable — yes  
8. A/B not reopened; A2 not continued — yes  
9. No production deploy / publish — yes  

---

## Callable required?

**Yes** — `completeStaffGangSheetAndOpenNext`

Reason: Firestore Rules keep Staff Gang Sheet **create/assign owner/admin-only**. Assigned helpers must complete cycle N and create N+1; a client transaction cannot create the next doc without weakening create Rules.

---

## Index required?

**Yes** — composite on `upcomingShows`:

`source ASC` + `assignedStaffUserId ASC` + `productionStatus ASC`

Used by the complete callable open-lane uniqueness query.

**Not deployed** in this Implement phase.

---

## Automated verification

| Check | Result |
|-------|--------|
| Focused unit/contract tests (29) | **pass** (exit 0) |
| Studio `tsc --noEmit` | **pass** (exit 0) |
| Functions `npm run build` | **pass** (exit 0) after local `npm install` in `functions/` |
| ESLint on touched TS files | **pass** (exit 0) |
| `git diff --check` | **fail** — pre-existing trailing whitespace in plan markdown (C+D amendment lines); not introduced by C/D code |
| Firestore Rules tests (`staffGangSheet.rules.test.ts`) | **not run** — emulator requires Java (`Could not spawn java -version`) |

---

## Unresolved blockers

1. **Owner DEV QA** for C + D (and still-pending B)  
2. **DEV deploy authorization** if live DEV Rules/Functions/indexes needed before QA (index + callable + Rules must be on the environment under test)  
3. **A2** Apple Developer ID + `MAC_CSC_*` (unchanged)  
4. Rules unit tests blocked locally without Java  

---

## Next FreshForge command

After owner records DEV QA results (or after authorized DEV deploy for QA):

`Continue Workflow` → Test phase for Studio 1.0.6 C+D (then Signoff when A2/B gates allow)

Do **not** mark overall goal DONE.
