# Checkpoint: DEV Firestore Rules deploy — Studio 1.0.4 smoke correctives A+B

| Field | Value |
|-------|-------|
| Date | 2026-08-12 |
| Authorization | `AUTHORIZE DEV FIRESTORE RULES DEPLOY: Studio 1.0.4 smoke correctives A+B to fresh-prints-dev` |
| Project | **fresh-prints-dev** |
| Target | `firestore:rules` only |
| Source branch/worktree | `hotfix/studio-1.0.4-smoke-correctives-a-b` |
| Result | **Deploy complete!** (exit 0) |
| Functions deploy | **not** performed (not required) |
| Production | **not** touched |
| Draft 369535832 | **not** mutated |

## What shipped to DEV

- `designArchiveStatusOnlyUpdate()` soft-archive fast path
- Approval path cheap-fails when target status is `archived`

## Owner DEV QA next

Use **local Studio** (hotfix worktree / branch) against **fresh-prints-dev**:

### Defect A
1. Import fully opaque PNG → reject with transparent-background message; no Storage/design/AI
2. Import transparent PNG → succeeds

### Defect B
3. As owner: Archive a rejected design from AI Review → succeeds
4. As helper (if available): Archive a rejected design → succeeds

Reply with `PASS` / `FAIL: …` / `PASS WITH NOTES: …`
