# STOP: Derivative locus full-Studio DEV diagnostic

| Field | Value |
|-------|-------|
| Date | 2026-08-13 |
| Managed goal | `studio-1.0.4-ai-processing-preview-cleanup-corrective` |
| Branch / HEAD | `fix/studio-1.0.4-ai-preview-cleanup-corrective` @ `c6e9235614b6816a98a71f998b47bd7fe18c371f` |
| Worktree | `C:\coding\fresh-prints-wt-studio104-corrective` |
| Status | **STOPPED — local DEV full-chain PASS; production locus still not proven; no primary fix** |
| Machine report JSON | `docs/workflow/reviews/2026-08-13-studio-1.0.4-derivative-locus-full-studio-dev-diagnostic.json` |

---

## Required return (1–24)

### 1. Current branch / HEAD
`fix/studio-1.0.4-ai-preview-cleanup-corrective` @ `c6e9235614b6816a98a71f998b47bd7fe18c371f`

### 2. Reproduction environment
- **Project:** `fresh-prints-dev` only (no production writes)
- **Method:** Automated full-chain harness mirroring Studio import stages:
  - Electron main Sharp encode (Studio thumb/preview params)
  - Electron IPC `Uint8Array` handoff + renderer `instanceof` assert
  - Client SDK original upload → design create → `imported→processing` → thumbnail WebP upload → preview WebP upload → path persist (`processing→imported` + paths)
  - Batch `UPLOAD_CONCURRENCY = 2`
- **Not run:** Interactive Studio `ImportsPage` file-picker UI (dialog cannot be driven headlessly). DEV stage instrumentation was added to Studio so a UI re-run will write JSONL under `~/.fresh-prints/diagnostics/`.

### 3. Batch input summary
| File | Bytes | Pixels |
|------|------:|--------|
| `deriv-locus-batch-1.png` | 11,538,046 | 4500×4500 alpha |
| `deriv-locus-batch-2.png` | 11,538,488 | 4500×4500 alpha |
| `deriv-locus-batch-3.png` | 11,539,443 | 4500×4500 alpha |
| `deriv-locus-batch-4.png` | 11,540,250 | 4500×4500 alpha |

Run id: `mss3o7sh`. Design ids (DEV leftovers if cleanup incomplete): `diagmss3o7sh0`…`diagmss3o7sh3`.

### 4. Whether local DEV reproduced
**NO** — all 4 batch designs completed with `pipelineSuccess=true`.

### 5. Single vs batch
Batch **PASS** → single-file follow-up **skipped** (not needed).

### 6. Per-design diagnostic stage table (summary)

All four designs shared the same PASS trail:

| Stage | Result |
|-------|--------|
| main read / sharp / thumb / preview | PASS |
| IPC handoff (`Uint8Array`) | PASS |
| original upload | PASS |
| design create | PASS |
| markDesignProcessing | PASS |
| uploadThumbnailWebp | PASS |
| uploadPreviewWebp | PASS |
| markDesignDerivativesComplete | PASS |
| final verify (paths + Storage objects) | PASS |
| rollback | not invoked |

Full stage trails: see companion JSON.

### 7. Derivative generation (actual Electron execution)
**PASS** for all 4 — thumb ~52 KB, preview ~520–526 KB WebP each.

### 8. IPC derivative handoff
**PASS** — post-IPC `instanceof Uint8Array` true; constructors `Uint8Array`.

### 9. markDesignProcessing
**PASS** (client `updateDoc` status `imported→processing`).

### 10. Thumbnail Storage upload
**PASS**

### 11. Preview Storage upload
**PASS**

### 12. Derivative-path persistence
**PASS** (status back to `imported` + `thumbnailPath`/`previewPath` set).

### 13. Rollback
**Not invoked** (no failures).

### 14. Exact first failing stage
**None on local DEV full-chain.** Production locus still unknown.

### 15. Exact sanitized error / error code
**N/A** (no failure).

### 16. Whether existing `derivativeError` already contained the answer
**N/A** for this DEV run (no derivative failure). Architecture note: pipeline failures do populate `derivativeError` on the import/batch result; Storage upload errors are currently **message-wrapped** and may **drop Firebase `code`** unless diagnostics capture it (instrumentation now attaches `firebaseCode` on wrapped Errors).

### 17. Whether error was hidden/discarded by UI
Repo check: `BatchImportResultPanel` **does** render a “Derivative failures” list using `file.derivativeError`. `ImportResultPanel` also shows `derivativeError`. So if the value exists in report state, UI already surfaces it — failure visibility corrective is about ensuring the string is retained/captured, not inventing a new panel from scratch.

### 18. Firestore Rules `4ac77141` → `975f640` impact conclusion
Diff is **archive-only**:
- Adds `designArchiveStatusOnlyUpdate`
- Excludes `status == "archived"` from `catalogApprovalStatusOnlyUpdate`
- Wires archive branch into `allow update`

**Cannot affect** `imported→processing` or `processing→imported` + `thumbnailPath`/`previewPath` writes used by import. Those paths either hit the approval allowlist (status-only subset without path fields) or fall through to `designRequiredFieldsValid` — **unchanged** by this diff. Do **not** classify Rules as causal for CASE 1 (structural non-impact; no emulator Rules regression required to reject Rules as the smoking gun).

### 19. Whether concurrency is implicated
**Not implicated by this DEV run.** Confirmed source: `UPLOAD_CONCURRENCY = 2`, `DERIVATIVE_PROCESSING_CONCURRENCY = 1`. Batch of 4 with concurrency 2 **passed**.

### 20. Whether packaged runtime evidence is still needed
**YES.** Per owner decision tree: local DEV PASS → do **not** conclude Windows-only yet → next is a **DEV-configured diagnostic packaged Studio build** (same packaging path as 1.0.4, connect only to `fresh-prints-dev`, **not** draft `369614747`, not published).

### 21. Proven root cause
**Not established.** Local DEV full-chain did not reproduce the production CASE 1 terminal state.

### 22. Recommended corrective
**Do not implement a speculative primary derivative fix.** Keep Option B approved but deferred. Next evidence step = packaged DEV diagnostic Studio build + same ≥4-file transparent batch import, with the new stage JSONL sink enabled (`FP_DERIVATIVE_LOCUS_DIAG=1` / unpackaged DEV logging).

### 23. Checkpoint path
- This doc: `docs/workflow/reviews/2026-08-13-studio-1.0.4-derivative-locus-full-studio-dev-diagnostic.md`
- Machine JSON: `docs/workflow/reviews/2026-08-13-studio-1.0.4-derivative-locus-full-studio-dev-diagnostic.json`

### 24. Recommended next FreshForge command

```text
CONTINUE WORKFLOW: PACKAGED DEV DIAGNOSTIC STUDIO BUILD
```

Constraints reminder: fresh-prints-dev only; do not mutate draft `369614747`; do not publish; do not implement primary fix until packaged (or UI) repro fails with a captured first stage + sanitized error.

---

## Instrumentation added (DEV-only; not the primary fix)

| Area | Change |
|------|--------|
| Main JSONL sink | `electron/services/import/derivativeLocusDiagnostic.ts` → `~/.fresh-prints/diagnostics/derivative-locus-*.jsonl` |
| IPC | `IMPORT_LOG_DERIVATIVE_LOCUS_DIAG` + preload bridge |
| Stages | enrich / generation / orchestration IPC return / derivative pipeline / rollback |
| Storage errors | wrapped Error retains `firebaseCode` for diagnostics |

---

## Honesty limits

- This run proves the **client Storage/Firestore + Electron IPC/encode chain** works for large transparent PNGs on DEV with batch concurrency 2.
- It does **not** prove the interactive Studio ImportsPage session/batch-job lifetime path is identical under every packaged Windows condition that produced the 1.0.4 smoke failure.
- Client cleanup of diagnostic designs hit `PERMISSION_DENIED` (design delete not staff-client-allowed). Leftover DEV docs/objects may remain for ids `diagmss3o7sh0`–`3` and temp staff user — owner may wipe via Admin/Test Data Reset later. **No production fixtures touched.**

---

## What was NOT done (correctly)

- No primary speculative derivative fix
- No Option B implementation
- No production fixture mutation / Start AI / delete
- No draft `369614747` rebuild/publish
- No production deploy / PR
