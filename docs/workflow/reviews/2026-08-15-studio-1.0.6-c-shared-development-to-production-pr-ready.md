# Development → Production PR — ready to open (agent blocked)

| Field | Value |
|-------|-------|
| Date | 2026-08-15 |
| Status | **STOP** — Cursor hook blocked `gh pr create` targeting `production` |
| Base | `production` @ `da5304e` |
| Head | `development` @ `0951075` (merge of PR #76) |
| Signed-off tip present | `0451bc4` + `9351a83` ancestors of `origin/development` — **yes** |
| Production unchanged | `origin/production` still `da5304e` — **yes** |

## Owner: open the PR

Compare: https://github.com/roasted-garlic/freshprints/compare/production...development?expand=1

Or:

```bash
gh pr create --base production --head development \
  --title "Promote Studio 1.0.6 C-SHARED lineage to production (backend gate; no Studio publish)" \
  --body-file docs/workflow/reviews/2026-08-15-studio-1.0.6-c-shared-development-to-production-pr-body.md
```

## Diff audit (matches preflight)

| Bucket | Present? |
|--------|----------|
| A C-SHARED backend/source | Yes — Rules, indexes, Staff + Portal-isolation Functions, queueTab sync |
| B Searchable Category | Yes |
| C AI Review thumbnail bg | Yes |
| D Studio 1.0.6 version/release source | Yes (`package.json` 1.0.6, update mapping, release workflow tests) |
| E Docs/workflow | Yes |
| F Unexplained / Portal / App Hosting / Storage / Algolia / secrets / DNS / Phase 9 | **None** |

## Firebase allowlist (unchanged)

```
firestore:rules
firestore:indexes
functions:createInitialStaffGangSheet
functions:completeStaffGangSheetAndOpenNext
functions:syncPrintRequestQueueTab
functions:listPortalAllocatableShows
functions:queuePortalPrintRequestToShow
functions:onShowAllocationCreated
functions:onPrintRequestItemQueueTabInputWritten
functions:onShowAllocationQueueTabInputWritten
```

Index ADD only: `upcomingShows` `source` ASC + `productionStatus` ASC.

## Authorization phrase

```
APPROVE PROD C-SHARED BACKEND PROMOTION: RULES+INDEXES+FUNCTIONS ALLOWLIST
```

Do **not** merge or deploy until that phrase is given.
