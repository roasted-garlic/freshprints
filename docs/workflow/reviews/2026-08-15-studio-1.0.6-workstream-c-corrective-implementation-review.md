# Implementation Review — Workstream C corrective (tab + Add Request)

**Date:** 2026-08-15  
**Goal:** `studio-mac-autoupdate-signing-and-searchable-category-picker`  
**Scope:** Narrow Studio renderer corrective for Staff Gang Sheets QA defects C1/C2  
**Branch:** `feature/studio-1.0.6-mac-signing-and-searchable-category`

## Owner QA input

| Workstream | Result |
|------------|--------|
| B | PASS |
| C | FAIL (C1 tab flicker, C2 missing Add Request) |
| D | PASS |

## Root causes

### C1 — Tab flicker
`UpcomingShowsPage` selection effect treated URL `selectedShowIdParam` as authoritative for `queueSurface`. Clicking **Staff Gang Sheets** cleared local selection but left a Whatnot show id in the URL → effect immediately set `queueSurface` back to **Shows**.

### C2 — Missing Add Request
Primary: C1 prevented staying on Staff detail. Secondary: only a low-visibility “+ Add Print Request” existed in the attached-requests card (not a header **Add Request**), and Staff manage capability was not used for the control gate.

## Corrective

- Explicit surface wins after hydration; incompatible URL show is cleared instead of flipping the tab
- Surface click clears URL selection via `updateSelectedShowPath(null)`
- Header + section **Add Request** for authorized open lanes; Staff uses `canManageStaffGangSheetShow`
- Allocation success reasserts Staff surface

## Verdict

**approved_with_notes** for this corrective slice.

Notes: Firebase DEV redeploy **not** required (renderer-only). Owner re-QA required before Test phase.

## Tests run

| Check | Exit |
|-------|------|
| Focused surface/Add Request/origin tests (18) | **0** |
| Studio `tsc --noEmit` | **0** |
| ESLint (touched UpcomingShows files) | **0** |
| `git diff --check` (touched paths) | **0** |

## Next

Owner re-run DEV QA checklist; do **not** start FreshForge Test until C PASS.
