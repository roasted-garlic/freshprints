# Studio 1.0.4 P4 — Production Firebase Deploy Record

**Date:** 2026-08-13  
**Status:** COMPLETE (both authorized artifacts)

## Authorization

Owner authorized (exact):

> Authorize production Firebase deploy for Studio 1.0.4 P4 (firestore:rules + functions:deleteEligibleUnapprovedDesign)

| Constraint | Value |
|------------|-------|
| Project | `fresh-prints-prod` ONLY |
| Artifacts | `firestore:rules`; `functions:deleteEligibleUnapprovedDesign` |
| Expected SHA | `e59205d7eccf0991e9a8a9b7be266cfeff831158` |
| Promote tip included | `57a29acdd3ee2c4eb3749c36860b9b90d78593ce` |

## Pre-deploy verification

| Check | Result |
|-------|--------|
| `git fetch origin` | OK |
| `origin/production` | `e59205d7eccf0991e9a8a9b7be266cfeff831158` (match) |
| Promote worktree align | Could not safely use promote tip (dirty `package-lock.json`; wrong branch) |
| Deploy worktree | Created `C:\coding\fresh-prints-wt-prod-deploy-studio104-p4` detached @ `e59205d7` |
| HEAD | `e59205d7eccf0991e9a8a9b7be266cfeff831158` |
| `designDerivativeCompletionUpdate` in `firestore.rules` | Present (fn + allow path) |
| `functions/src/index.ts` exports `deleteEligibleUnapprovedDesign` | Yes |
| `functions/src/deleteEligibleUnapprovedDesign.ts` | Exists |
| Firebase project targeting | Deploy via `--project fresh-prints-prod` (`.firebaserc` maps `production` → `fresh-prints-prod`; default CLI alias remains `fresh-prints-dev`) |
| Diagnostic bake | `STUDIO_DIAGNOSTIC_BUILD` unset; `PACKAGED_DERIVATIVE_LOCUS_DIAG` defaults false without flags |

## Deploy results

### 1) firestore:rules

| Field | Value |
|-------|-------|
| Worktree | `C:\coding\fresh-prints-wt-prod-deploy-studio104-p4` |
| Command | `firebase deploy --only firestore:rules --project fresh-prints-prod` |
| Exit code | `0` |
| Success snippets | `cloud.firestore: rules file firestore.rules compiled successfully`; `firestore: released rules firestore.rules to cloud.firestore`; `Deploy complete!` |
| Project Console | https://console.firebase.google.com/project/fresh-prints-prod/overview |

### 2) functions:deleteEligibleUnapprovedDesign

| Field | Value |
|-------|-------|
| Worktree | `C:\coding\fresh-prints-wt-prod-ae-deploy` (detached @ `e59205d7`; used because deploy worktree lacked local `.env.fresh-prints-prod` required for non-interactive params) |
| Command | `firebase deploy --only functions:deleteEligibleUnapprovedDesign --project fresh-prints-prod` |
| Env | `FUNCTIONS_DISCOVERY_TIMEOUT=120` |
| Exit code | `0` |
| Success snippets | `functions source uploaded successfully`; `functions[deleteEligibleUnapprovedDesign(us-central1)] Successful create operation.`; `Deploy complete!` |
| Post-verify | `firebase functions:list --project fresh-prints-prod` shows `deleteEligibleUnapprovedDesign` v2 callable `us-central1` 512MB nodejs20 |

### Function deploy notes (non-broadening)

- Fresh deploy worktree required `npm install` (root + functions) for `tsc` / shared `firebase` types.
- First function attempts from clean worktree failed on missing deps, discovery timeout, then missing dotenv params — **no other Firebase artifacts deployed**.
- Function create succeeded from ae-deploy worktree aligned to the same production SHA with existing local `.env.fresh-prints-prod` (params only; secrets not rotated/deployed).

## Explicit non-actions (confirmed)

- Storage / indexes / other functions / App Hosting / Algolia / secrets: **not deployed**
- Production fixtures: **untouched**
- Draft `369614747`: **untouched**
- Studio release create/publish: **not performed**
- Force push: **not performed**

## Facts for next step

| Item | Value |
|------|-------|
| PR #70 | Merged to production @ `e59205d7` |
| Production Firebase Rules | **DEPLOYED** to `fresh-prints-prod` |
| `deleteEligibleUnapprovedDesign` | **DEPLOYED** to `fresh-prints-prod` |
| Fixtures | Untouched |
| Draft 369614747 | Untouched |
| **NEXT** | **NEW** Studio 1.0.4 dual-platform release build from `e59205d7` (**do not reuse** draft 369614747) |
