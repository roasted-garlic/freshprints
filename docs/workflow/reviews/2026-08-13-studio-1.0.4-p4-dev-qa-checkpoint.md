# Manual Test Checkpoint: Studio 1.0.4 P4 DEV QA (owner)

| Field | Value |
|-------|-------|
| Date | 2026-08-13 |
| Managed goal | `studio-1.0.4-ai-processing-preview-cleanup-corrective` |
| Environment | **fresh-prints-dev** |
| Implementation commit | `5e0b072faab46e07a7011278e2c903f7513e77fa` |
| Implementation Review | `docs/workflow/reviews/2026-08-13-studio-1.0.4-p4-derivative-completion-implementation-review.md` |
| Status | **Owner PASS** (2026-08-13) — QA A pipeline + QA C Option B (incl. list refresh + diagnostic banner off) |

---

## Deployed to DEV (authorized only)

| Artifact | Command | Exit | Result |
|----------|---------|------|--------|
| Firestore Rules | `firebase deploy --only firestore:rules --project fresh-prints-dev` | **0** | Released `firestore.rules` to `fresh-prints-dev` |
| Function | `firebase deploy --only functions:deleteEligibleUnapprovedDesign --project fresh-prints-dev` | **0** | Created `deleteEligibleUnapprovedDesign` (us-central1) |

No Storage Rules, indexes, App Hosting, Algolia, other Functions, or production deploy.

---

## QA build / run instructions

Use Studio source at commit **`5e0b072`** (worktree branch `fix/studio-1.0.4-ai-preview-cleanup-corrective`) against **fresh-prints-dev**.

```bash
# From apps/studio — ensure Studio points at fresh-prints-dev (existing .env.local / project config)
npm run dev --workspace @fresh-prints/studio
```

Or a normal local Studio run you already use for DEV. Packaged rebuild is **not** required for this checkpoint unless you prefer it.

Prerequisites:

- Owner account on `fresh-prints-dev`
- Valid transparent PNG fixtures (DEV-only; not production smoke files)
- Confirm Firebase project is DEV before importing

---

## QA A — P4 import → derivatives → automatic AI

**Why automated tests are insufficient:** Real Imports UI + Storage + Rules + AI queue against live DEV.

### Steps
1. Imports → multi-select valid transparent PNGs → import → **Expected:** import completes without derivative failure
2. Open AI Processing → **Expected:** queue thumbnails appear
3. Select a design → **Expected:** large preview appears (not Preview Pending / Derivatives incomplete)
4. Confirm badge/status is **not** “Derivatives incomplete”
5. Wait without clicking Start AI → **Expected:** automatic AI starts; designs progress toward Needs Review
6. Confirm no permission-denied and no derivative rollback (paths + WebPs remain)

### Pass criteria
- [ ] Full pipeline success for imported set
- [ ] Thumbs + preview visible
- [ ] Automatic AI without Start AI
- [ ] No permission-denied / rollback

---

## QA B — Failure visibility

**Why automated tests are insufficient:** UX presentation of partial success.

### Steps
1. Exercise the reviewed DEV-safe failure scenario (do **not** weaken Rules or touch production)
2. Confirm failed derivative pipeline is **not** presented as healthy WAITING FOR AI
3. Confirm **Derivatives incomplete** (or reviewed failure indication) appears
4. Confirm meaningful derivative error is visible in import results
5. Confirm Start AI unavailable when derivative paths are absent
6. Confirm recovery guidance (re-import / owner safe-delete) — no misleading full-success state

### Pass criteria
- [ ] No healthy WAITING FOR AI for missing derivatives
- [ ] Error visible; Start AI gated

---

## QA C — Option B safe delete

**Why automated tests are insufficient:** Owner confirmation UX + live Storage/Firestore cleanup.

Use **DEV-only** eligible unapproved designs (imported/processing). Never production fixtures.

### Steps
1. Design Library (non-archived) as owner → select eligible design(s) → Permanently delete
2. Confirm confirmation phrase UX (`DELETE UNAPPROVED DESIGNS`)
3. Confirm delete succeeds for eligible design: Storage original/thumb/preview removed; Firestore doc removed
4. Attempt non-eligible (e.g. ready) or referenced design → **Expected:** fail closed
5. Confirm no unrelated design affected

### Pass criteria
- [x] Eligible delete works end-to-end
- [x] List removes deleted row immediately (local reconcile; no stale reload lag)
- [ ] Protected designs denied *(optional / not re-blocked)*
- [x] No collateral deletes

---

## Owner results (2026-08-13)

| Check | Result |
|-------|--------|
| QA A — import → derivatives → auto AI | **PASS** (prior session) |
| QA C — Option B delete on AI Processing | **PASS** after UI discoverability + list-refresh fix |
| Diagnostic banner gone (no `VITE_FP_DERIVATIVE_LOCUS_DIAG`) | **PASS** |

### Please reply with
- `PASS` — all QA A/B/C criteria met
- `FAIL: [description]` — what failed
- `PASS WITH NOTES: [notes]` — acceptable with follow-ups

---

## Explicitly out of scope this checkpoint

- Production Rules/Functions deploy
- Production 8-fixture cleanup
- Draft `369614747`
- Studio rebuild/publish
- Production PR
