# Test Report: Studio 1.0.6 C-SHARED Internal Gang Sheets

| Field | Value |
|-------|-------|
| Date | 2026-08-15 |
| Tester | Test Agent |
| Plan | docs/workflow/plans/2026-08-14-studio-mac-autoupdate-signing-and-searchable-category-picker-plan.md |
| Implementation | C-SHARED + post-QA fixes (queueTab sync/remove UX, create permissions, cycle numbering) |
| Overall | **passed_with_notes** |

---

## Summary

Focused C-SHARED automated checks are green after fixing stale contract expectations (staff create allowed; cycle number on create) and Studio typecheck errors in the queueTab sync / client create fallback paths. Owner DEV QA already **PASS**. Firestore rules emulator could not run on this machine (Java missing); rules coverage remains a noted gap for CI/local emulator environments.

---

## Commands Run

| Check | Command | Exit Code | Result | Notes |
|-------|---------|-----------|--------|-------|
| Unit / contract | `npx tsx --test` (C-SHARED focused set — 10 files) | 0 | pass | 53 tests |
| Typecheck | `npx tsc --noEmit` (from `apps/studio/`) | 0 | pass | Fixed `result.queueTab` + unknown field narrowing |
| Lint | `npm run lint` | 0 | pass | Fixed exhaustive-deps on Internal Sheets hydration effect |
| Functions build | `npm --prefix functions run build` | 0 | pass | |
| Studio Vite build | `npx vite build` (from `apps/studio/`) | 0 | pass | |
| Backend / rules | `firebase emulators:exec --only firestore "npx tsx --test tests/firebase/staffGangSheet.rules.test.ts"` | 1 | skip | Java not installed (`Could not spawn java -version`) |
| Portal typecheck / build | — | — | skip | Portal isolation covered by contract tests; no Portal UI change in this Test pass |
| E2E | — | — | skip | Not configured for this flow |
| Full monorepo unit sweep | — | — | skip | Scoped to C-SHARED surfaces |

### Focused unit/contract file set

- `packages/shared/src/utils/staffGangSheet.test.ts`
- `packages/shared/src/utils/printRequestQueueTabRecompute.test.ts`
- `apps/studio/.../permissionService.staffGangSheet.test.ts`
- `apps/studio/.../upcomingShowService.staffGangSheet.contract.test.ts`
- `apps/studio/.../showQueueStaffGangSheetUi.contract.test.ts`
- `apps/studio/.../AddToShowModal.staffGangSheet.contract.test.ts`
- `apps/studio/.../showQueuePrintRequestSources.test.ts`
- `functions/src/createInitialStaffGangSheet.contract.test.ts`
- `functions/src/completeStaffGangSheetAndOpenNext.contract.test.ts`
- `functions/src/staffGangSheetPortalIsolation.contract.test.ts`

---

## Failures (if any)

None remaining after in-scope fixes during Test phase:

1. Contract tests expected owner-only create / empty `createStaffGangSheetLane(user, {})` — updated to match staff-allowed create + cycle number input.
2. Studio typecheck: `callTracedFunction` returns unwrapped `Response` (use `result.queueTab`); narrowed `unknown` Firestore fields in client create fallback.
3. Lint: added `lockedSurface` / `navigate` to hydration `useEffect` deps.
4. Rules unit test still expected helper create deny / maxTotalQuantity deny on create — updated to allow helper create and optional capacity; assignee/whatnotShowId denies retained.

---

## Skipped Checks

| Check | Reason |
|-------|--------|
| Firestore rules emulator (`staffGangSheet.rules.test.ts`) | Java runtime missing on agent host; file also not yet in root `npm run test:rules` script |
| Portal typecheck/build | C-SHARED Test scoped to Studio + Functions; Portal isolation asserted via contract tests |
| Full unit sweep / E2E | Out of C-SHARED focused scope; no E2E suite for this flow |

---

## Manual Testing

| Test | Status | Notes |
|------|--------|-------|
| Owner DEV QA checklist (C-SHARED) | pass | Recorded 2026-08-15 — “everything is as it should be” |
| Queued ↔ Working after allocate/remove | pass | Confirmed in owner QA after cache/optimistic fix |

Manual checklist: `docs/workflow/reviews/2026-08-15-studio-1.0.6-workstream-c-shared-owner-qa-checklist.md`

---

## Recommendations

- Add `tests/firebase/staffGangSheet.rules.test.ts` to `npm run test:rules` once Java/emulator is available in CI.
- Ensure DEV deploy includes `syncPrintRequestQueueTab` if not already live (owner QA passed with current DEV).

---

## Signoff Readiness

- [x] All required automated checks pass OR failures documented
- [x] Manual tests complete OR checkpoint pending
- [x] Ready for signoff phase

**Next step:** signoff
