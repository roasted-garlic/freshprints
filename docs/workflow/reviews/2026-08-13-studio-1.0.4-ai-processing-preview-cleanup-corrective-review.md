# Formal Review: Studio 1.0.4 AI Processing + Preview + Cleanup Corrective

| Field | Value |
|-------|-------|
| Date | 2026-08-13 |
| Reviewer | Review Agent |
| Plan | `docs/workflow/plans/2026-08-13-studio-1.0.4-ai-processing-preview-cleanup-corrective-plan.md` |
| Verdict | **approved_with_changes** |

---

## Summary

Read-only production evidence proves all eight smoke-window designs have missing WebP derivatives and null `thumbnailPath`/`previewPath`, with `aiProcessingStage=null`. Failures A and B share CASE 1 (incomplete derivative pipeline / empty paths); automatic AI correctly did not enqueue. Cleanup gap C is accurately documented. Plan is implementable after the listed required changes (product confirmation on Option B + fixture allowlist, and implement must pin the Windows derivative failure step before coding a speculative fix).

---

## Checklist

| Area | Status | Notes |
|------|--------|-------|
| Scope clear and bounded | pass | A/B/C + 1.0.4 release implications only |
| Architecture alignment | pass | Studio/Portal separation; Admin destructive path |
| Security impact addressed | pass | Owner-only delete; no broad Rules expansion |
| Data model impact addressed | pass | Optional failure fields; hard-delete allowlist |
| Backend impact addressed | pass | Callable likely; Rules likely none |
| Test strategy adequate | pass | Automated + DEV + Windows QA + dual smoke |
| Human checkpoints identified | pass | Full list including cleanup + publish |
| Roadmap alignment | pass | Corrective amendment to 1.0.4 smoke, not new phase |
| Documentation plan | pass | Plan records evidence; handoff path absent |
| No silent scope expansion | pass | Draft 369614747 retained; no Portal |

---

## Architecture Review

**Findings:**

- Import partial-success contract (`importSuccess` without `pipelineSuccess`) is real and explains the owner “successful import” vs empty previews contradiction.
- Background AI gating on `pipelineSuccess` is correct; Start AI must not become the product fix for broken derivatives.
- Packaged Windows disables pipeline/AI queue traces — production diagnosis correctly relied on Admin Firestore/Storage evidence.

**Required changes:**

- [x] During Implement: identify the **exact** failing step (sharp load vs encode vs upload vs persist) via local/DEV repro before shipping a fix; do not guess “Windows Storage auth” given CASE 1 evidence.

---

## Security Review

**Findings:**

- Client Storage staff rules are sufficient for non-ready thumbs when objects/paths exist; not the primary defect here.
- Hard-delete must be Admin callable + owner-only + fail-closed reference checks.

**Required changes:**

- [ ] None beyond plan’s fail-closed contract

**Human approval needed before production:**

- [x] Fixture cleanup auth (exact IDs)
- [x] Functions deploy if new callable
- [x] New draft publish (separate)

---

## Data Model Review

**Findings:** Evidence tables complete for eight IDs. Recommend cleanup allowlist includes `prayer` and duplicate `faithoverfeartimothy`.

**Required changes:**

- [x] Owner confirm cleanup allowlist = all 8 IDs in plan table (default if no objection: **all 8**)

---

## Backend Review

**Findings:**

- `enqueueAiEnrichment` path requirement matches evidence (never queued).
- `purgeArchivedDesignAssets` is insufficient for C.
- Firestore/Storage Rules deploy likely **none** if Admin-only delete.

**Required changes:**

- [ ] None

---

## Cleanup options comparison

| Option | Pros | Cons |
|--------|------|------|
| **A** One-time exact-ID cleanup | Narrowest for incident; auditable | Leaves product gap for next failed smoke |
| **B** Permanent owner hard-delete | Studio production-safe; reusable | More implement scope; must stay fail-closed |

**Verdict:** Plan recommendation **A+B** accepted — B for product, A executed via B’s guards on the allowlist.

**Required changes:**

- [x] Before Implement: owner explicitly accepts Option B in this corrective (or rejects → Plan revises to A-only with documented residual risk).

---

## Test Review

**Findings:** Coverage list adequate. Exact npm/vitest commands left `[NEEDS REPO CHECK]` — acceptable for Plan; Implement must fill.

**Required changes:**

- [x] Implement fills exact test file paths/commands in test report

---

## Required changes before Implement (summary)

1. Owner accept **Option B** (or revise Plan to A-only).
2. Owner confirm **8-ID** cleanup allowlist (include `prayer` + second `faithoverfeartimothy`).
3. Implement must **prove** the Windows derivative failure locus (generation vs upload vs persist) before coding the primary fix.
4. Do not mutate fixtures / draft / production until authorized later gates.

---

## Verdict

**approved_with_changes**

Plan may proceed to Implement only after items 1–2 are recorded (item 3 is an Implement-phase constraint, not a Plan rewrite blocker).

---

## Remaining human checkpoints

1. Owner Plan acceptance + Option B + allowlist confirmation
2. (Implement later) DEV Functions/Rules as needed
3. Owner DEV QA
4. PR merge auth
5. Prod Functions deploy auth
6. Fixture cleanup auth
7. New dual-platform draft
8. Windows / Mac arm64 / Mac x64 Big Sur smoke
9. Publish auth

---

## Recommended next FreshForge command

After owner replies accepting Plan + Option B + 8-ID allowlist:

`Continue Workflow` → Implement (still no Start AI / no fixture delete / no draft mutation until later gates)
