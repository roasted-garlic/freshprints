# STOP: Derivative failure locus evidence gate (Studio 1.0.4 corrective)

| Field | Value |
|-------|-------|
| Date | 2026-08-13 |
| Managed goal | `studio-1.0.4-ai-processing-preview-cleanup-corrective` |
| Branch | `fix/studio-1.0.4-ai-preview-cleanup-corrective` @ `c6e9235614b6816a98a71f998b47bd7fe18c371f` |
| Status | **STOPPED — locus not proven; no primary derivative fix implemented** |
| Owner decisions recorded | Plan accepted; Option B accepted; 8-ID allowlist confirmed (future cleanup only) |

---

## Why stopped

Formal Review + owner Implement instructions require pinning the exact derivative failure stage **before** coding the primary fix, and forbid speculative fixes based only on CASE 1 terminal state.

Current evidence proves **CASE 1 terminal state** and narrows candidates, but does **not** yet identify the exact failing step with a sanitized error string.

---

## Already proven (unchanged)

| Fact | Evidence |
|------|----------|
| Production tip | `c6e9235` (Portal-only after Studio build SHA `975f640`) |
| Draft `369614747` | Still draft/unpublished @ `975f640` — untouched |
| 8 fixtures | originals exist; `thumbnailPath`/`previewPath` empty; WebP objects absent; `aiProcessingStage=null` |
| A+B | Shared CASE 1 / incomplete derivative pipeline |
| Auto AI | Correctly gated on `pipelineSuccess` — never reached `queued` |

### Timestamp discrimination (Firestore)

| Pattern | Fixture(s) | Implication |
|---------|------------|-------------|
| `createdAt == updatedAt` | `jesusmysavior` (`09wKlenmbdbKkdvd8Mph`) | No post-create write → likely **no `derivatives` on IPC read** (generation failed / missing) before `markDesignProcessing` |
| `updatedAt` ~2.5–4s after `createdAt` | other 7 | Post-create write(s) occurred → pipeline likely **entered** (`markDesignProcessing` and/or revert/persist attempt) → failure more likely at **thumbnail/preview upload** or **Firestore path persist + Storage rollback** |

This suggests **possibly more than one stage** across the batch, not a single proven locus.

---

## Reproduction attempts this session

| Attempt | Result |
|---------|--------|
| Local Node `sharp` WebP encode (synthetic transparent PNGs) | **PASS** (encode OK) |
| Electron 30.5.1 IPC `Uint8Array` `instanceof` after `contextBridge` | **PASS** (not the defect on local Electron) |
| Read-only Admin download of prod original + encode probe | **BLOCKED** by Cursor `beforeShellExecution` (prod project string) even though owner authorized Plan/Implement evidence gate |
| Packaged Windows Studio console / `derivativeError` capture | **Not available** (agent cannot run owner packaged smoke UI) |
| Mac packaged repro | **Not run** (paused; no Mac agent environment) |

### Ruled out / weak as sole explanation

| Hypothesis | Status |
|------------|--------|
| Client Storage ACL as sole cause of A+B | Weak — objects missing + empty paths; Admin AI never queued |
| IPC drops derivatives while keeping original bytes after successful clone | No code path; full IPC failure would fail import (not matching) |
| `instanceof Uint8Array` post-IPC on local Electron 30 | Disproven locally (probe exit 0) — still untested on **packaged** Windows build of draft `369614747` |
| Batch cancel skipping all files | Disproven (8 successful catalog creates) |
| Empty-path mapping bug as root cause | Disproven (intentional create placeholders) |

### Still open (ranked)

1. Main-process derivative generation failure for some files (`derivativeError` on read) — fits equal-timestamp fixture
2. Thumbnail/preview Storage upload failure after generation — fits timestamp-gap fixtures
3. `markDesignDerivativesComplete` failure then Storage rollback — fits timestamp-gap + no WebP objects
4. Packaged-Windows-only sharp/runtime behavior not visible in local Electron/Node probes

---

## What was NOT done (correctly)

- No speculative primary derivative fix
- No production fixture mutation / Start AI / archive / delete
- No DEV/prod deploy
- No PR
- No draft `369614747` mutation/rebuild/publish
- Option B delete UI/callable **not** started (blocked behind evidence gate per “prove locus before primary fix”; WS2/WS3 remain pending after gate clears)

---

## Owner actions to unblock Implement

Provide **one** of the following:

### Option 1 — Packaged / local Studio error string (preferred)

Re-import **one** similar transparent PNG on:

- local `npm run dev` Studio, and/or
- packaged Windows 1.0.4 draft `369614747`

Then paste the import UI / DevTools **`derivativeError`** (or batch “imported without completed derivatives” detail) with secrets redacted.

Reply format:

`DERIVATIVE LOCUS EVIDENCE: [exact sanitized error text]`
`ENVIRONMENT: [local-dev | packaged-windows-369614747 | other]`

### Option 2 — Allow read-only prod original probe

Allow Cursor shell for:

```bash
cd functions
$env:FIREBASE_PROJECT_ID='fresh-prints-prod'
node scripts/studio-104-derivative-locus-probe.mjs
```

(Read-only download of one fixture original + local sharp encode; **no** Storage/Firestore writes.)

Phrase:

`AUTHORIZE READ-ONLY PROD ORIGINAL DERIVATIVE LOCUS PROBE`

### Option 3 — Confirm local-dev full import succeeds

If local Studio against `fresh-prints-dev` imports a large transparent PNG with thumbs+preview+auto AI, reply:

`LOCAL DEV IMPORT DERIVATIVES: PASS`

That proves packaged-or-prod-config specificity and redirects the locus hunt.

---

## After locus is proven

Resume `Continue Workflow` → Implement Workstreams 1–3 (narrow fix + visibility + Option B delete), tests, Implementation Review, **STOP before DEV deploy**.

---

## Artifacts

- Branch/worktree: `C:\coding\fresh-prints-wt-studio104-corrective`
- Probe scripts (local only):
  - `apps/studio/scripts/probe-derivative-ipc-bytes.cjs`
  - `apps/studio/scripts/probe-derivative-ipc-preload.cjs`
  - `functions/scripts/studio-104-derivative-locus-probe.mjs` (blocked this session)
- Prior inventory: `functions/scripts/studio-104-smoke-fixture-readonly-inventory.mjs`
