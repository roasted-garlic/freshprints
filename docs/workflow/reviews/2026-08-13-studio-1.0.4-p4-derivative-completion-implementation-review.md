# Implementation Review: Studio 1.0.4 P4 derivative path persistence + failure visibility + Option B

| Field | Value |
|-------|-------|
| Date | 2026-08-13 |
| Reviewer | Implementation / Review Agent |
| Managed goal | `studio-1.0.4-ai-processing-preview-cleanup-corrective` |
| Plan | `docs/workflow/plans/2026-08-13-studio-1.0.4-ai-processing-preview-cleanup-corrective-plan.md` |
| Formal Review | `docs/workflow/reviews/2026-08-13-studio-1.0.4-ai-processing-preview-cleanup-corrective-review.md` |
| P4 checkpoint | `docs/workflow/reviews/2026-08-13-studio-1.0.4-packaged-dev-diagnostic-ui-results-p4.md` |
| Branch | `fix/studio-1.0.4-ai-preview-cleanup-corrective` |
| Baseline HEAD (uncommitted implement) | `c6e9235614b6816a98a71f998b47bd7fe18c371f` |
| Verdict | **approved_with_notes** — STOP before DEV deploy |

---

## Summary

Primary P4 defect fixed with a narrow Firestore Rules fast path authorizing the exact `markDesignDerivativesComplete` mutation. Failure visibility reconciles missing derivative paths in AI Processing and strengthens import partial-success messaging. Option B owner-only Admin hard-delete callable + Studio Design Library UI implemented fail-closed. Automated Rules / unit / typecheck / lint / `git diff --check` green. **No DEV or production deploy performed.**

---

## Checklist

| Area | Status | Notes |
|------|--------|-------|
| Scope within approved plan + P4 amendment | pass | Rules + visibility + Option B; draft 369614747 untouched |
| Architecture | pass | Client mutation fixed in Rules; delete via Admin callable |
| Security | pass | Narrow fast path; owner-only delete; no Storage Rules change |
| Tests honest | pass | Commands recorded below |
| Human checkpoints remaining | pass | DEV deploy auth + DEV QA + later prod |

---

## Required return matrix

| # | Item | Result |
|---|------|--------|
| 1 | Branch | `fix/studio-1.0.4-ai-preview-cleanup-corrective` |
| 2 | Exact HEAD | Baseline `c6e9235614b6816a98a71f998b47bd7fe18c371f` — **implement changes uncommitted** (owner commit when ready) |
| 3 | P4 Rules root cause | `processing→imported` + `thumbnailPath`/`previewPath` fell through approval allowlist + full `designRequiredFieldsValid`, hitting expression-budget / deny as `permission-denied` after Storage uploads succeeded → rollback deleted WebPs |
| 4 | Predicate | `designDerivativeCompletionUpdate()` in `firestore.rules` |
| 5 | Allowed mutation shape | Source `imported`\|`processing` → target `imported`; affectedKeys only `status`, `thumbnailPath`, `previewPath`, `updatedAt`, `updatedBy`, optional `createdBy` heal; `updatedBy==auth.uid`; timestamp `updatedAt` |
| 6 | Denied boundaries | ready/rejected/archived/queued/unknown sources; wrong/empty paths; `originalPath`/title/tags/AI/purge extras; non-staff/customer/unauth; spoofed `updatedBy`; purged assets |
| 7 | Canonical path enforcement | Exact `/thumbnails/{id}.webp` and `/previews/{id}.webp` via `request.resource.data.id` |
| 8 | Pre-fix regression | DENY without fast path (stripped OR → `false`) on enrichment-heavy processing→imported+paths |
| 9 | Post-fix regression | ALLOW processing→imported and imported→imported + heal; DENY suite green |
| 10 | Full Rules suite | `npm run test:rules` → **124 pass / 0 fail** |
| 11 | Studio import tests | AI queue sequencing + auto-AI regression + inbox/queue eligibility tests **pass** |
| 12 | Automatic AI regression | `importAutoAiAfterDerivatives.regression.test.ts` proves `pipelineSuccess` → `onDesignPipelineSuccess` → `enqueueImportedDesignsForBackgroundAi` → `enqueueForProcessing` |
| 13 | Failure visibility | Import panels warn on partial success; AI status `derivatives_incomplete`; badge “Derivatives incomplete”; Start AI gated via `isDesignAwaitingAiStart` |
| 14 | Failed-pipeline recovery | No dedicated derivative retry — UI directs re-import or owner safe-delete (**product gap noted**) |
| 15 | Option B summary | Callable `deleteEligibleUnapprovedDesign` + Design Library owner multi-select hard-delete dialog |
| 16 | Safe-delete guards | Owner-only; status allowlist imported\|processing; deny ready/unknown; deny mid-AI stage; deny printRequestItems / showAllocations / companionLinks / companion denorm / sourceCustomerUpload; Storage original+thumb+preview then Firestore delete; confirm phrase; max 25 |
| 17 | Studio files | Rules consumers: `aiProcessingOutput*`, import result panels, Design Library delete UI, permissions, gated derivative locus diagnostics retained |
| 18 | Functions files | `functions/src/deleteEligibleUnapprovedDesign.ts` (+ export in `index.ts`); contract test |
| 19 | Rules files | `firestore.rules` + `tests/firebase/designDerivativeCompletion.rules.test.ts` + `package.json` `test:rules` |
| 20 | Diagnostics | Gated `derivativeLocusDiagnostic` + `STUDIO_DIAGNOSTIC_BUILD` bake **retained** (off by default); Studio package version restored to `1.0.4`; probe scripts remain untracked local artifacts — do not commit |
| 21 | Migration impact | **None** — Rules allow existing client write; no data migration |
| 22 | Storage Rules impact | **None** |
| 23 | Automated tests | See commands below |
| 24 | Studio typecheck | `npx tsc -p apps/studio/tsconfig.json --noEmit` → **exit 0** |
| 25 | Functions build | `cd functions && npm run build` → **exit 0** |
| 26 | Lint | `npm run lint` → **exit 0** |
| 27 | `git diff --check` | **exit 0** |
| 28 | This review path | `docs/workflow/reviews/2026-08-13-studio-1.0.4-p4-derivative-completion-implementation-review.md` |
| 29 | Verdict | **approved_with_notes** (STOP before deploy; commit pending) |
| 30 | DEV deploy matrix | See below |
| 31 | Owner DEV QA checklist | See below |
| 32 | Next human checkpoint | Authorize DEV deploy of Rules + `deleteEligibleUnapprovedDesign` only |
| 33 | Recommended FreshForge command | After commit: `Continue Workflow` → Test/Signoff with DEV deploy auth, **or** explicit `Authorize DEV deploy: firestore:rules + deleteEligibleUnapprovedDesign` |

---

## Automated test commands / results

```text
JAVA_HOME=.tools/jdk npm run test:rules
→ 124 pass / 0 fail

npx tsx --test packages/shared/src/utils/deleteEligibleUnapprovedDesignValidation.test.ts \
  apps/studio/src/renderer/src/features/ai-review/utils/aiProcessingQueue.test.ts \
  apps/studio/src/renderer/src/features/ai-review/utils/aiReviewInbox.test.ts \
  apps/studio/src/renderer/src/features/imports/services/importAutoAiAfterDerivatives.regression.test.ts \
  apps/studio/src/renderer/src/features/imports/services/importAiBackgroundQueueSequencing.test.ts \
  functions/src/deleteEligibleUnapprovedDesign.contract.test.ts
→ 47 pass / 0 fail

npx tsc -p apps/studio/tsconfig.json --noEmit → exit 0
cd functions && npm run build → exit 0
npm run lint → exit 0
git diff --check → exit 0
```

---

## Exact DEV deploy matrix (STOP — awaiting owner auth)

| Artifact | Deploy? | Notes |
|----------|---------|-------|
| `firestore:rules` | **YES** | Primary P4 fix |
| Function `deleteEligibleUnapprovedDesign` | **YES** | Option B only — do not deploy unrelated Functions |
| Storage Rules | **NO** | Unchanged |
| Other Functions | **NO** | |
| Studio rebuild/publish | **NO** this checkpoint | After DEV Rules+Function + QA; new draft later |
| Production | **NO** | |
| Draft `369614747` | **NO** — untouched | |

Suggested (after auth):

```bash
firebase deploy --only firestore:rules --project fresh-prints-dev
firebase deploy --only functions:deleteEligibleUnapprovedDesign --project fresh-prints-dev
```

---

## Owner DEV QA checklist (after deploy)

1. Packaged or local Studio against **fresh-prints-dev**
2. Imports → multi-select transparent PNGs → import
3. Expect: full pipeline success; thumbs + preview paths persist; WebPs remain; no rollback; `pipelineSuccess=true`; automatic AI without Start AI; Needs Review progression
4. Intentional/fake derivative failure path: import UI shows partial success + error; AI Processing shows **Derivatives incomplete**, not ordinary Waiting for AI; Start AI unavailable
5. Option B: owner selects eligible imported DEV-only fixtures → confirm `DELETE UNAPPROVED DESIGNS` → doc + Storage removed; deny ready / referenced designs
6. Do **not** clean production 8 fixtures in this pass

---

## Notes / follow-ups

- Commit implement work before deploy PR.
- Production Rules + Function promotion and 8-fixture cleanup remain separate human checkpoints.
- New Studio 1.0.4 dual-platform draft required after production promotion (draft 369614747 stays failed evidence).
