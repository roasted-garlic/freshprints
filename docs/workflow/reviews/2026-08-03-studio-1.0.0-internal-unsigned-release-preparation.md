# Checkpoint: Studio 1.0.0 internal-unsigned release preparation (Phase G)

Date: 2026-08-03
Production baseline: `e544de8ff17edef1b44f6fc21ff4c5afe1e4572a` (verified: `origin/production`
matched exactly before any change)
Version change: `1.0.0-beta.5` → `1.0.0`

## Verdict: READY FOR OWNER APPROVAL

This checkpoint prepares the version-bump-only change required before the stable Studio release
workflow can be run. **No release, tag, installer publication, or production action was created or
performed.**

## Phase G1 — production baseline verification

- `origin/production` confirmed exactly `e544de8ff17edef1b44f6fc21ff4c5afe1e4572a`.
- Working tree clean before any change.
- Studio version confirmed `1.0.0-beta.5` before this change.
- Confirmed the internal-unsigned policy is present on `production`: `distribution_mode` input
  (`signed` | `internal-unsigned`, default `signed`) exists in
  `.github/workflows/studio-release.yml`.
- Confirmed all six `PROD_FIREBASE_*` checks remain mandatory and fail closed (source inspection).
- Confirmed the stable-source guard (`git merge-base --is-ancestor HEAD origin/production`)
  remains intact and unmodified.

## Phase G2 — release branch and version change

Branch: `release/studio-1.0.0-internal-unsigned`, created directly from `origin/production` at the
exact commit above (not from local/stale `development`).

**Files changed (exactly two, both version metadata):**

- `apps/studio/package.json`: `"version": "1.0.0-beta.5"` → `"version": "1.0.0"`
- `package-lock.json`: the `apps/studio` workspace entry's `"version"` field, synchronized via
  `npm install --workspaces=false --package-lock-only` (not hand-edited) — exactly one line
  changed, confirmed via `git diff --stat`.

No other version-bearing file was found (`grep` for the old version string across
`apps/studio/**/*.{json,json5,ts,tsx}` returned no matches beyond the two files above). No
updater runtime, workflow behavior, Firebase configuration, packaging configuration, Studio UI,
Portal code, Rules, indexes, Functions, or Storage Rules were modified.

## Phase G3 — stable release safeguards (re-confirmed at this exact commit)

| Safeguard | Status |
|---|---|
| Stable source guard (`git merge-base --is-ancestor`) | intact, unmodified |
| Production reachability check | intact, unmodified |
| All six `PROD_FIREBASE_*` checks fail closed | confirmed via source; no fallback to `DEV_FIREBASE_*` for stable |
| Generated packaged channel literal `stable` | confirmed via local generation + compiled-bundle inspection |
| `distribution_mode: internal-unsigned` allows missing signing only when explicitly selected | confirmed via source (unchanged from the merged policy) |
| Partial signing configuration still fails closed | confirmed via source (unchanged) |
| `signed` remains the default | confirmed (`default: signed` in the workflow input) |
| Prerelease behavior unchanged | confirmed — `distribution_mode` is ignored for `release_type: prerelease` |
| GitHub Release remains draft-first | confirmed via source (unchanged) |
| Automatic updater download disabled | confirmed (`autoDownload = false`, unchanged) |
| Download / Restart to Update remain explicit user actions | confirmed (unchanged) |
| Automatic update installation remains silent after approval | confirmed (`quitAndInstall(true, true)`, the only call site, unchanged) |
| Manual first installation remains assisted | confirmed (`oneClick: false`, unchanged) |

## Phase G4 — full non-publishing verification

| Check | Result |
|---|---|
| Root dependency install | exit 0 |
| Functions dependency install | exit 0 |
| Functions build | exit 0 |
| Repo lint | exit 0, 0 warnings |
| Portal typecheck | exit 0 |
| Portal production build | exit 0 |
| Studio typecheck | exit 0 |
| Updater/release-note/generator + signing-policy tests | **49/49 pass** |
| Stable channel generation | literal `"stable"` confirmed |
| Prerelease channel generation | literal `"prerelease"` confirmed (unaffected, re-verified) |
| Production Firebase fail-closed | confirmed via source, unchanged from prior verification |
| **Studio production package build (non-publishing)** | exit 0 — produced `Fresh Prints-Windows-1.0.0-Setup.exe` locally, **using a synthetic, clearly-non-real `.env.local`** (`VITE_FIREBASE_API_KEY=TEST-SYNTHETIC-VALUE-NOT-REAL`, project `fresh-prints-prod`) — no real GitHub secret or real production credential was used or exposed anywhere in this verification |
| `git diff --check` | exit 0 |

The real local `apps/studio/.env.local` (development config) was backed up before this test,
overwritten with the synthetic test file, and **restored exactly afterward** — confirmed via a
direct `head` check post-restore. No real credential was ever read, logged, or committed.

### Packaged evidence (extracted from the actual built `app.asar`)

- **Channel:** compiled main-process bundle contains `function bs() { return "stable"; }` — a
  literal value, no environment-variable lookup.
- **Firebase project:** renderer bundle contains `VITE_FIREBASE_PROJECT_ID:"fresh-prints-prod"` —
  the synthetic test project ID, confirming the *selection logic* correctly resolves to whatever
  project's config file is present at build time (this will be the real `fresh-prints-prod` values
  when the actual `PROD_FIREBASE_*` GitHub secrets are used by the real workflow run).
- **Dev-reference check:** `grep -rl "fresh-prints-dev"` against the extracted bundle found two
  matches, both confirmed to be pre-existing, unrelated, safe application logic — a UI help-text
  string on `TestDataResetPage.tsx` and a `const ja = "fresh-prints-dev"` dev-project-ID allowlist
  constant used to gate dangerous test-data tooling away from any build not connected to
  `fresh-prints-dev` (the check is `!e.isPackaged && ... projectId == ja`, i.e. it only ever
  matters for *unpackaged dev builds*, and its presence is what correctly *disables* that tooling
  in a `fresh-prints-prod`-configured build). **Zero occurrences of the real dev API key**
  (`AIzaSyC-0HXqrZ...`) were found anywhere in the bundle — confirmed via direct grep, count 0
  across every asset file.
- **Expected stable assets confirmed generated locally:** `Fresh Prints-Windows-1.0.0-Setup.exe`,
  its blockmap, and `latest.yml` (all present in `apps/studio/release/1.0.0/`, gitignored, not
  committed, deleted after inspection).
- **Signing status of this local test build:** unsigned (no `CSC_LINK`/`WIN_CSC_LINK` present
  locally) — this matches the exact expected outcome of `distribution_mode: internal-unsigned`
  when the real workflow runs with no signing secrets configured.

No GitHub release, tag, or publish action was created. No artifact was uploaded anywhere.

## Secret-name existence status

Unchanged from the Phase F audit: **`OWNER CONFIRMATION REQUIRED BEFORE WORKFLOW RUN`.** This
environment has no GitHub API authentication and cannot check whether the six `PROD_FIREBASE_*`
secrets exist. This does not block preparing this version PR, but it does block running the
actual stable workflow — the owner must confirm secret existence via GitHub Settings → Secrets and
variables → Actions → Repository secrets before dispatching `release_type: stable`.

## Confirmation

No stable workflow was run. No GitHub tag or Release was created or published. No Firebase
deployment, App Hosting rollout, production data change, DNS action, or custom-domain action
occurred. This checkpoint changed only two version-metadata files and added this documentation.
