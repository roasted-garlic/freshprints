# Production deploy commands — owner execute (agent hook-blocked)

| Field | Value |
|-------|-------|
| Date | 2026-08-15 |
| Authorization | `APPROVE PROD C-SHARED BACKEND PROMOTION: RULES+INDEXES+FUNCTIONS ALLOWLIST` |
| Source on production | PR **#77** merged — `9f945f3` |
| Signed-off tip | `0451bc4` is ancestor of `origin/production` |
| Agent status | Cursor hooks block `firebase deploy --project fresh-prints-prod` and `gh pr merge` |

## Exact commands (run from repo root, after `npm --prefix functions run build`)

```bash
firebase deploy --only firestore:rules --project fresh-prints-prod

firebase deploy --only firestore:indexes --project fresh-prints-prod

firebase deploy --only functions:createInitialStaffGangSheet,functions:completeStaffGangSheetAndOpenNext,functions:syncPrintRequestQueueTab,functions:listPortalAllocatableShows,functions:queuePortalPrintRequestToShow,functions:onShowAllocationCreated,functions:onPrintRequestItemQueueTabInputWritten,functions:onShowAllocationQueueTabInputWritten --project fresh-prints-prod
```

## After deploy

1. Wait until `upcomingShows` index (`source` ASC + `productionStatus` ASC) is **READY**.
2. Run production smoke from preflight (Whatnot queue, Portal allocatable, Staff invisible to Portal).
3. Do **not** publish Studio 1.0.6.
4. Reply `Continue Workflow` for A2 (or paste deploy output for agent to record).

## Rollback

Redeploy Rules/Functions from pre-promote SHA `da5304e`.
