# Checkpoint: Option B UI discoverability + diagnostic release cleanliness

| Field | Value |
|-------|-------|
| Date | 2026-08-13 |
| Managed goal | `studio-1.0.4-ai-processing-preview-cleanup-corrective` |
| Prior commit | `5e0b072faab46e07a7011278e2c903f7513e77fa` |
| Status | **Owner PASS** — Delete on Processing; immediate list remove; diagnostic banner OFF |

---

## Why owner could not find Option B (at 5e0b072)

| # | Finding |
|---|---------|
| Location | **Design Library only** (`/designs` / Design Library page) — **not** AI Processing |
| Label | Toolbar **Permanently delete (N)** after selecting card checkboxes |
| Eligibility UI | Owner + non-archived library + not request-selection mode; status `imported`\|`processing` |
| Why missing on Processing | No wiring on AI Review / Processing workspace at 5e0b072 |
| Missing derivatives | Does **not** hide the action (status-based only) |
| Stale DEV fixtures | Backend-eligible if `imported`/`processing` and fail-closed refs pass |

### Alternate library path (still valid)

1. Open **Design Library** (not Archived)
2. As owner, checkboxes appear on cards
3. Select eligible design(s)
4. Click **Permanently delete (N)**
5. Type `DELETE UNAPPROVED DESIGNS`

---

## Minimal correction

Expose **Permanently delete** on **AI Processing** for the selected eligible design, reusing the same dialog/service/callable.

Also: diagnostic banner no longer auto-enables on ordinary Vite DEV / unpackaged Electron — only intentional diagnostic flags.

---

## Exact owner QA C steps (after localhost restart)

1. Restart Studio so this UI correction loads
2. AI Processing → select a stale broken imported/processing design
3. Confirm **Permanently delete** (danger) appears in the actions row
4. Click → confirmation dialog → type `DELETE UNAPPROVED DESIGNS`
5. Confirm design removed from Processing + Storage/Firestore cleaned on DEV
6. Confirm ready / referenced designs fail closed (optional)

No DEV Function redeploy required (UI-only).

---

## Production promotion matrix (verified)

| Artifact | Required | Notes |
|----------|----------|-------|
| Studio source | **YES** | UI + gated diagnostics |
| `functions:deleteEligibleUnapprovedDesign` | **YES** | Admin callable |
| Firestore Rules | **YES** (overall P4 release) | Derivative completion fast path |
| Storage Rules | **NO** | Unchanged |
| Migration | **NO** | |

Not DEV-only tooling.

---

## Diagnostic release cleanliness

| Check | Result after correction |
|-------|-------------------------|
| Banner on ordinary DEV | **OFF** (flag-gated only) — owner **PASS** after clearing `.env.local` |
| Banner on normal packaged release | **OFF** (`PACKAGED_DERIVATIVE_LOCUS_DIAG=false`) |
| Production Firebase forced to DEV | **No** (diagnostic package only) |
| Must fail release if banner shows | Documented gate for final 1.0.4 draft |

Strings still exist in source for intentional diagnostic packages only — must not bake `STUDIO_DIAGNOSTIC_BUILD=1` / `VITE_FP_DERIVATIVE_LOCUS_DIAG=1` into release.
