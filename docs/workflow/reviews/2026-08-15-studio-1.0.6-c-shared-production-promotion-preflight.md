# Production Promotion Preflight — Studio 1.0.6 C-SHARED (STOP before mutation)

| Field | Value |
|-------|-------|
| Date | 2026-08-15 |
| Goal | `studio-mac-autoupdate-signing-and-searchable-category-picker` |
| Scope | C-SHARED production **source + backend** promotion only |
| Studio 1.0.6 publish | **HELD** for A2 |
| Status | **PREFLIGHT COMPLETE — STOP before any production mutation** |

---

## 1. Current git / source state

| Item | Value |
|------|-------|
| Current branch | `feature/studio-1.0.6-mac-signing-and-searchable-category` |
| Local HEAD | `0508934` — `feat(studio): shared Staff Gang Sheets for 1.0.6 C-SHARED` |
| `origin/development` | `c84d45b` — `docs(signoff): close AI Review reprocess goal after Studio 1.0.5` |
| `origin/production` | `da5304e` — `Merge pull request #75 from roasted-garlic/development` (Studio **1.0.5**) |
| Working tree | **DIRTY** — ~43 modified/untracked paths (signed-off C-SHARED QA correctives **not committed**) |
| Feature vs development | **6 commits** on feature not in `origin/development` |
| Feature vs production | development tip is in production history; feature is **not** in production |

**Hard gate:** signed-off C-SHARED is **not** fully on `HEAD`. Uncommitted / untracked includes Rules, Functions (`syncPrintRequestQueueTab`, create/complete callables), Studio queueTab remove UX, signoff/test artifacts, etc. Do **not** promote `0508934` as the final signed-off tip.

---

## 2. Exact signed-off C-SHARED commit range (committed so far)

Commits on feature not in `origin/development` (oldest → newest):

| SHA | Summary | C-SHARED? |
|-----|---------|-----------|
| `180844f` | searchable categories + install-phase update errors; Studio version **1.0.6** | **No** (A1 / update UX) |
| `2a6ef85` | Staff Gang Sheets **+** AI Review queue background sync | **Partial** (C + D) |
| `70941f6` | restore C+D plan amendment docs | docs |
| `d412b1a` | Staff Gang Sheets DEV deploy QA docs | docs |
| `7ff80f3` | Staff tab selection + Add Request corrective | **Yes** (C corrective) |
| `0508934` | shared Staff Gang Sheets C-SHARED | **Yes** |

**Plus:** large uncommitted post-`0508934` QA corrective set (must be committed before promotion). Call that future tip **`C-SHARED-FINAL`** (not yet created).

---

## 3. Production diff summary (committed `origin/production...HEAD`)

**Backend / shared (C-SHARED relevant):**

- `firestore.rules` — `staff_gang_sheet` source, create/update helpers, Portal-safe constraints
- `firestore.indexes.json` — **add** `upcomingShows`: `source` ASC + `productionStatus` ASC
- Functions: `createInitialStaffGangSheet`, `completeStaffGangSheetAndOpenNext`, Portal isolation edits (`listPortalAllocatableShows`, `queuePortalPrintRequestToShow`, `onShowAllocationCreated`, `portalCalendarShowVisibility`)
- `packages/shared` staff gang sheet utils/types

**Also in the same range (not C-SHARED-only):**

- Studio searchable category picker + Select filter
- Studio install-phase update error mapping / release workflow tweaks
- AI Review wallpaper/background sync
- Studio `package.json` version `1.0.5` → `1.0.6`
- Studio Internal Sheets UI (dormant for end users until 1.0.6 publish)

**Portal app (`apps/portal`):** **no file changes** in `origin/production...HEAD`.

**Uncommitted backend still required for signed-off behavior:**

- `functions/src/syncPrintRequestQueueTab.ts` (**new**)
- `functions/src/lib/printRequestQueueTab.ts` (**new**)
- Updates to create/complete callables, Rules (staff create, capacity), queueTab triggers refactor, Studio remove/cache UX

---

## 4. Unrelated-change audit

| Bundle | Enters a full feature→development→production PR? | Risk if source merges before Studio publish |
|--------|--------------------------------------------------|-----------------------------------------------|
| A1 searchable categories | Yes | Source-only on `production` branch; **not** installed until Studio 1.0.6 publish |
| D AI Review wallpaper sync | Yes | Same — client-only until 1.0.6 |
| Studio version 1.0.6 in package.json | Yes | Does **not** publish installer by itself |
| C-SHARED Studio UI | Yes | 1.0.5 clients ignore new routes/callables |
| C-SHARED Firebase | Yes (intended) | Live after Firebase deploy |

**Verdict:** A full lineage merge promotes **more than C-SHARED backend**. That is acceptable for **source** if owner accepts A1/D landing on `production` without Studio publish, **or** owner may request a backend-only cherry-pick PR. Recommended default: merge full reviewed feature lineage (includes A1/D source) + **scoped Firebase deploy** (C-SHARED allowlist only). Do **not** dispatch Studio release.

**App Hosting:** Portal root unchanged; automatic rollouts historically **disabled**. Still: **do not** trigger App Hosting rollout as part of this promotion.

---

## 5. Backward compatibility — Studio v1.0.5

| Concern | Verdict |
|---------|---------|
| 1.0.5 Whatnot Show Queue | **Safe** — Rules remain Whatnot-capable; additive Staff source |
| 1.0.5 does not call new Staff callables | **Safe** — unused endpoints |
| Portal list/queue | **Safer** — explicit reject/skip `staff_gang_sheet` (no Staff docs expected in prod yet) |
| Recently Requested | **Safe** — Staff allocations skipped (none in prod initially) |
| New index | **Safe** — additive; wait READY before relying on Staff queries |
| Generate / Mark finished | **Safe** — `gangSheetGeneratedAt` fields optional in Rules; no production requirement that 1.0.5 Generate before finish was removed in a way that breaks 1.0.5 writes |
| Backend before 1.0.6 client | **ALLOWED** — intended rollout order |

**Coupling STOP?** **No.** Backend may precede Studio 1.0.6 publish.

---

## 6. PR / promotion plan (after clean committed tip)

1. **Commit** all signed-off C-SHARED correctives on the feature branch → `C-SHARED-FINAL`.
2. **PR** `feature/studio-1.0.6-mac-signing-and-searchable-category` → `development` (reviewed merge; no force-push).
3. **PR** `development` → `production` (protected; no force-push; no direct rewrite).
4. After merge to `production`: deploy Firebase allowlist to `fresh-prints-prod` (commands below).
5. Wait until new index **READY**.
6. Owner production smoke (checklist below).
7. **Hold** Studio 1.0.6 publish; return workflow to **A2**.

Do not create a redundant content merge if commits already exist on the target — verify with `git merge-base` / PR files changed after step 1.

---

## 7. Exact Firebase production deployment allowlist

Project: **`fresh-prints-prod`** only. No App Hosting, Storage, Algolia, secrets, DNS.

```bash
firebase deploy --only firestore:rules --project fresh-prints-prod

firebase deploy --only firestore:indexes --project fresh-prints-prod

firebase deploy --only \
  functions:createInitialStaffGangSheet,\
  functions:completeStaffGangSheetAndOpenNext,\
  functions:syncPrintRequestQueueTab,\
  functions:listPortalAllocatableShows,\
  functions:queuePortalPrintRequestToShow,\
  functions:onShowAllocationCreated,\
  functions:onPrintRequestItemQueueTabInputWritten,\
  functions:onShowAllocationQueueTabInputWritten \
  --project fresh-prints-prod
```

Notes:

- Include queueTab trigger pair **only if** final commit changes those exports (current uncommitted tree does).
- Do **not** bare `firebase deploy --only functions`.
- `portalCalendarShowVisibility` is a **library** used by callables — deployed via the callable packages above, not a separate Function.

---

## 8. Exact index changes

| Action | Collection | Fields |
|--------|------------|--------|
| **ADD** | `upcomingShows` | `source` ASC, `productionStatus` ASC |

**Remove:** none in `origin/production...HEAD` (no assignee composite present in repo indexes vs production). Do not invent deletions.

Post-deploy: wait until this composite is **ENABLED / READY** in Firebase Console before declaring backend promotion complete.

---

## 9. Migration / data verdict

| Question | Answer |
|----------|--------|
| Production migration required? | **No** |
| Copy DEV Staff fixtures? | **No** |
| Create production Staff sheets during deploy? | **No** |
| C-SHARED previously usable in prod? | **No** — feature was not shipped |

---

## 10. Rollback plan

1. **Rules:** redeploy previous production Rules from `origin/production` tip at time of promote (record pre-deploy SHA).
2. **Indexes:** additive index may remain (harmless); do not delete unless owner-approved cleanup.
3. **Functions:** redeploy previous versions of the allowlisted Functions from pre-promote SHA / prior Cloud Functions revision.
4. **Source:** revert via follow-up PR to `production` (no force-push).
5. **Data:** no Staff sheet creation expected — nothing to purge if smoke creates none.

---

## 11. Production smoke checklist (after authorized deploy)

- [ ] Whatnot Show Queue loads in production Studio 1.0.5
- [ ] Existing production shows remain usable (open / allocate / timer paths unchanged)
- [ ] Portal allocatable show flow works normally
- [ ] Staff Gang Sheets invisible to Portal list
- [ ] Direct Portal queue onto a Staff id rejected (if a Staff doc were present; otherwise confirm callable reject path still healthy)
- [ ] Owner/admin Studio Show Queue: no permission errors
- [ ] No existing show docs converted/mutated by deploy
- [ ] New `source + productionStatus` index **READY**
- [ ] Deployed Functions healthy (no crash loops; create/complete/sync callable reachable for staff)

Do **not** create unnecessary production customer data. Do **not** create production Internal sheets unless owner explicitly wants a controlled smoke create (default: **skip create**).

---

## 12. Studio 1.0.6 publication hold

**Confirmed held.** No `studio-release` dispatch, no stable 1.0.6 publish, no internal-unsigned-as-stable bypass, no Squirrel signature validation bypass. A2 Developer ID signing remains open before stable 1.0.6.

---

## 13. Owner authorization phrase

After the working tree is committed and PRs are ready, authorize production mutation with:

```text
APPROVE PROD C-SHARED BACKEND PROMOTION: RULES+INDEXES+FUNCTIONS ALLOWLIST
```

Optional separate phrases if splitting gates:

```text
APPROVE PROD PR MERGE: C-SHARED LINEAGE TO PRODUCTION
APPROVE PROD FIREBASE DEPLOY: C-SHARED ALLOWLIST
```

**Do not** interpret this preflight as authorization.

---

## 14. Exact next FreshForge command

**Now (pre-mutation):** owner must authorize committing the signed-off dirty tree (or commit themselves), then:

```text
Continue Workflow
```

After commit + development PR merge + production PR ready, reply with the authorization phrase above to proceed with Firebase production deploy only.

**After verified prod backend smoke:** 

```text
Continue Workflow
```

→ Workstream **A2** (Mac Developer ID signing credential checkpoint).

---

## STOP

No production PR merge, Firebase deploy, index delete, Studio release dispatch, or Studio 1.0.6 publish was performed in this step.
