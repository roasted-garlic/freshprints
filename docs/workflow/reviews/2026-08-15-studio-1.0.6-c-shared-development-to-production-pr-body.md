## Summary
Promote reviewed `development` (`0951075`, merge of PR #76) to `production` for **C-SHARED Shared Internal Gang Sheets** source + backend readiness.

Includes on this lineage:
- **A1** Searchable Category picker (already approved)
- **D** AI Review queue thumbnail / Halftone artwork-background sync (already approved)
- **C-SHARED** signed-off Internal Gang Sheets (owner DEV QA PASS; Test passed_with_notes; Signoff approved_with_notes)
- Studio package version **1.0.6** in source only

## Explicit holds
- **DO NOT** dispatch or publish Studio **1.0.6** (A2 Mac Developer ID signing still open)
- **DO NOT** treat merge alone as Firebase deploy — production Firebase is a **separate** owner-authorized allowlist deploy
- **No** App Hosting rollout, Storage, Algolia, secrets, or DNS in this promotion

## Firebase production allowlist (after merge + owner phrase)
```
firestore:rules
firestore:indexes
functions:createInitialStaffGangSheet
functions:completeStaffGangSheetAndOpenNext
functions:syncPrintRequestQueueTab
functions:listPortalAllocatableShows
functions:queuePortalPrintRequestToShow
functions:onShowAllocationCreated
functions:onPrintRequestItemQueueTabInputWritten
functions:onShowAllocationQueueTabInputWritten
```

Index **ADD only:** `upcomingShows` `source` ASC + `productionStatus` ASC. Wait READY before smoke complete.

## Diff audit vs preflight
Matches `docs/workflow/reviews/2026-08-15-studio-1.0.6-c-shared-production-promotion-preflight.md`:
- No `apps/portal` / App Hosting / Storage / Algolia file changes
- Backend + Studio 1.0.6 source + docs only
- Rollback base: `da5304e`

## Compatibility
Studio **v1.0.5** remains safe with backend preceding 1.0.6 client publish.

## Authorization required to merge + deploy
```
APPROVE PROD C-SHARED BACKEND PROMOTION: RULES+INDEXES+FUNCTIONS ALLOWLIST
```

## Test / QA
- [x] C-SHARED automated + owner DEV QA PASS
- [ ] After authorized merge+deploy: production smoke (Whatnot queue, Portal allocatable, Staff invisible to Portal, index READY)
