# Manual smoke: Studio Mac arm64 1.0.4 (draft `369614747`)

| Field | Value |
|-------|-------|
| Date | 2026-08-13 |
| Platform | **macOS Apple Silicon (arm64)** |
| Draft release ID | **`369614747`** |
| Build SHA | `975f6400262a86600c4662f39480c6f55e20b0c1` |
| Environment | Production `fresh-prints-prod` + Algolia search-only `Z1FVCM5QUX` / `portal_catalog_ready_prod` |
| Why automated tests are insufficient | Gatekeeper, ad-hoc signature, sharp/native, and production workflows require a real Mac |

## Exact artifact (must match)

| Item | Value |
|------|-------|
| File | `Fresh-Prints-Mac-arm64-1.0.4-Installer.dmg` |
| Size | `136840452` bytes |
| SHA-256 | `f87c90802679d0dea2f8122fcfd1072f48e7262c231f7ffe24af3fc036fc3bd7` |
| Download | Draft `369614747` only — https://github.com/roasted-garlic/freshprints/releases/tag/untagged-d31b85ddaa62bf6f9b78 |

Do **not** reuse older arm64 draft smoke. Verify SHA-256 before install.

## Gatekeeper / signature rules

- **Expected:** unverified / Open Anyway / right-click → Open friction for internal ad-hoc distribution — **acceptable**
- **Must NOT:** report prior broken-signature **“damaged” / “corrupt”** condition
- Launch only via documented Gatekeeper open procedure (Open Anyway / right-click Open)
- **Do not** clear quarantine (`xattr -d com.apple.quarantine`) as a substitute for a broken product signature
- After open: verify sharp/native via an import / image-processing path

## Prerequisites

- Apple Silicon Mac; install from **arm64** DMG only (not x64)
- Confirm production login (not DEV)

## Steps

1. Mount DMG / install app from the artifact above → **Expected:** install completes
2. Gatekeeper open procedure → **Expected:** unverified warning OK; app opens; **no** damaged/corrupt dialog
3. Login to production → **Expected:** auth succeeds; no DEV markers
4. Design Library → **Expected:** loads
5. Search / filter → **Expected:** works; no crash
6. Imports → **Expected:** UI usable
7. Transparent-image import → **Expected:** accepted (exercises sharp/native)
8. Opaque-image import → **Expected:** rejected with clear error
9. Folder selection → **Expected:** works
10. ZIP import → **Expected:** works or fails safely with clear error
11. AI Review → **Expected:** reachable for eligible items
12. Approve / reject / archive → **Expected:** succeed on allowed roles
13. Customer Uploads → **Expected:** usable
14. Print Requests → **Expected:** usable
15. Show Queue → **Expected:** usable for authorized role
16. Export / download → **Expected:** completes for a known path
17. Updater / update check → **Expected:** does not crash
18. Mac window / menu behavior → **Expected:** menus/windows usable; no hang on common actions
19. Quit / relaunch → **Expected:** clean quit; relaunch works after Gatekeeper approval already granted

## Pass criteria

- [ ] Artifact SHA-256 matched
- [ ] No damaged/corrupt signature failure
- [ ] Launch after Gatekeeper procedure (no quarantine-clear workaround)
- [ ] Import/image path proves sharp/native OK
- [ ] Functional coverage above PASS
- [ ] Prior draft arm64 results not counted

### Please reply with

- `MAC ARM64 SMOKE 1.0.4: PASS`
- `MAC ARM64 SMOKE 1.0.4: FAIL: [description]`
- `MAC ARM64 SMOKE 1.0.4: PASS WITH NOTES: [notes]`
