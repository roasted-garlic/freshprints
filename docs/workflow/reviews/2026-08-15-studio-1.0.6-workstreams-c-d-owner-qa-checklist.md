# Owner DEV QA — Studio 1.0.6 Workstreams C + D

**Environment:** local Studio against DEV Firebase (after authorized DEV deploy of Rules + Functions + indexes if not already present)  
**Branch:** `feature/studio-1.0.6-mac-signing-and-searchable-category`

## Prerequisites / DEV deploy authorization

If DEV does not yet have:

- updated `firestore.rules`
- callable `completeStaffGangSheetAndOpenNext`
- composite index `upcomingShows`: source + assignedStaffUserId + productionStatus

**STOP and authorize** a **DEV-only** deploy of those three before QA. Do **not** deploy production.

Reply with: `AUTHORIZE DEV DEPLOY: rules+functions+indexes for Staff Gang Sheets QA`

---

## Workstream C — Staff Gang Sheets

### Pass criteria
- [ ] Show Queue has **Shows | Staff Gang Sheets**
- [ ] Owner/admin can create Staff Gang Sheet #1 and assign a helper (no hardcoded users)
- [ ] Helper sees only own assigned lane; cannot mutate another helper’s lane
- [ ] Staff sheet shows Requests/Prints; **no** Whatnot ID/URL, capacity bar, Full, override
- [ ] Add Request lists studio_internal + studio_customer only (no portal_customer)
- [ ] Portal customer cannot see/select Staff sheets; direct queue rejected if attempted
- [ ] Mark Complete on #N opens #N+1 for same assignee; double-click does not create two opens
- [ ] History lists prior completed Staff cycles
- [ ] Export ZIP / gang sheet still works on Staff allocations
- [ ] Ordinary Whatnot show capacity/import/Portal behavior unchanged

### Please reply with
- `PASS` / `FAIL: …` / `PASS WITH NOTES: …`

---

## Workstream D — AI Review left-rail background

### Pass criteria
- [ ] Select transparent design; change preview background → left thumbnail updates immediately to same color
- [ ] Other queue rows unchanged
- [ ] Switch selection → no cross-design leakage
- [ ] No reload/refetch required

### Please reply with
- `PASS` / `FAIL: …` / `PASS WITH NOTES: …`

---

## A2 status (unchanged)

Blocked on Apple Developer ID certificate + `MAC_CSC_LINK` / `MAC_CSC_KEY_PASSWORD`. Do not configure secrets in this QA.
