# Plan: Studio release version bump 1.0.4 → 1.0.5

| Field | Value |
|-------|-------|
| Date | 2026-08-14 |
| Goal | `studio-1.0.5-release-version-bump` (closeout corrective for Design Library production build detectability) |
| Parent | `studio-design-library-archive-restore-reconciliation` |
| Status | complete |

---

## Goal

Bump Fresh Prints Studio package version from **1.0.4** to **1.0.5** so existing 1.0.4 installs can detect the corrected production build as an update. Release metadata only — no product behavior changes.

---

## Scope

### In
- `apps/studio/package.json` → `1.0.5`
- Root `package-lock.json` `@fresh-prints/studio` workspace version → `1.0.5`
- `.github/workflows/studio-release.yml` finalize hard-gate `Expected Studio version` **1.0.4 → 1.0.5** (required or stable finalize fails)
- Align Mac stable error string that names `1.0.4` for the same gate semantics

### Out
- Product/runtime source
- Portal, Functions, Rules, indexes, Storage, App Hosting, Phase 9
- Firebase deploy
- Touching historical `v1.0.4*` tags/releases
- Unrelated lockfile dependency versions (e.g. sharp `1.0.4` packages)

---

## Repo check (version-bearing)

| File | Role |
|------|------|
| `apps/studio/package.json` | Source of truth (`electron-builder` / workflow `studio_version`) |
| `package-lock.json` `apps/studio.version` | Workspace metadata alignment |
| `.github/workflows/studio-release.yml` | Hard fail if version ≠ pinned expected string |

Not version sources for this bump: `electron-builder.json5` (uses `${version}`), comments, merge-yml test fixtures, unrelated npm package versions.

---

## Test strategy
- Studio typecheck + build
- Repo lint
- `git diff --check`
- Confirm diff is metadata-only
- Confirm workflow gate string is `1.0.5`

## Human checkpoints
- Protected PR `development` → `production` — **STOP before merge**
- Later: Studio 1.0.5 stable release dispatch (separate)

## Risks
| Risk | Mitigation |
|------|------------|
| Leaving workflow gate at 1.0.4 | Update gate in same commit |
| Accidental product edits | Diff review; touch only listed files |

## Rollback
Revert the version-bump commit / close PR without merge.
