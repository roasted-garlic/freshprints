# Risk Register — Fresh Prints

---

## Active Risks

| ID | Risk | Severity | Likelihood | Mitigation | Owner | Status |
|----|------|----------|------------|------------|-------|--------|
| R-001 | Generated installers/binaries in git pollute remote and confuse reviewers | High | High | Run `git-generated-output-cleanup` before push; document in DEPLOYMENT.md | Team | open |
| R-002 | No `npm test` — signoffs rely on lint/manual only | Medium | High | Add test runner phase; update TESTING.md | Team | open |
| R-003 | Firebase Storage rules may not be deployed in all environments | Medium | Medium | Verify per Phase 3C signoff C1; document in setup guide | `[NEEDS HUMAN INPUT]` | open |
| R-004 | Doc drift after rapid Phase 3 delivery | Medium | Medium | Intake + managed phase doc updates; ROADMAP header discipline | Team | mitigated |
| R-005 | Native `sharp` module build failures on new dev machines | Medium | Medium | Document in `docs/workflow/setup/electron-security-setup.md` | Team | open |
| R-006 | Secrets in local `.env.local` — must never commit | High | Low | `.gitignore` covers `.env.local`; pre-push review | Team | monitored |
| R-007 | Public Portal artwork uploads (abuse, ZIP bombs, private art leakage) | High | Medium | ADR-FP-073; server finalize; customer limits + daily caps; rules before UI; SVG deferred; no public derivative reads for unapproved uploads; abandoned cleanup callable (source orphans only); wipe target `customerUploads` on allowlisted dev | Team | open |
| R-008 | Dual assets after promote (upload production + design originals) | Low | Medium | Documented; request print uses upload path; catalog uses design paths; wipe designs ≠ wipe uploads | Team | accepted |
| R-009 | Upload `catalogReviewStatus` stays `sent_to_ai_review` after design approve/reject | Low | High | By design (ADR-FP-073); outcome on `designs`; intake shows promotedDesignId | Team | accepted |
| R-010 | Etsy website HTML scrape for in-app listing cards (ToS / legal + ops fragility) | High | Medium | **Mitigated (2026-07-16):** Owner rejected scrape quality; ADR-FP-087j rips scrape. Listings restored via Open API (ADR-FP-087l), not scrape. | Owner | mitigated |
| R-011 | Email trigger crash after provider acceptance could retry outside the provider idempotency window | Medium | Low | Deterministic Firestore job, transactional lease/attempt state, stable Resend idempotency key, bounded retries, provider message ID audit; monitor failed jobs and do not claim permanent exactly-once delivery | Team | monitored |
| R-012 | `sharp@0.33.5` (installed, `functions`/`apps/studio`) inherits a high-severity libvips advisory (CVE-2026-33327/33328/35590/35591, GHSA-f88m-g3jw-g9cj). Reachability review: 3 of 4 CVEs are confined to VIPS-native, 32-bit GIF, and TIFF decode paths not used by Fresh Prints; the fourth (CVE-2026-35590) is a DoS-only (crash, not memory-disclosure/RCE) null-pointer dereference in EXIF tag-group parsing, and `customerUploadProcessing.ts` calls `sharp(...).metadata()` on customer-uploaded PNG/WEBP bytes, which does read embedded EXIF — so a malformed EXIF block in a customer upload can crash the processing Function instance. Pre-dates Wave C; not introduced by it. | High | Low | Fix requires `sharp@0.35.3` (semver-major) across `functions` and `apps/studio`; needs its own reviewed upgrade/test pass, not an in-place bump. Interim: Cloud Functions auto-restarts a crashed instance; no data compromise. | Team | open |
| R-015 | Post-publication owner Portal QA (against live generation-4 snapshots) found: (1) the tag modal showing the full ~1,122-tag approved taxonomy with no per-tag ready-design count and no zero-result exclusion; (2) searching "BEST" showing only 1 of 2 matching designs until "Load more"; (3) a ~3,600-read Firestore Product Usage increase. Root causes: `listApprovedTags()`'s generated path returned the full client-safe taxonomy with no facet/count data (no equivalent to the pre-Wave-C client-hydration-derived facet existed); its first-pass "fallback" was reported as bounded but review found it still queried the full `tags` **and** `designs` collections unbounded; and `listMatchingDesigns()` first sorted candidate IDs alphabetically by design ID, silently replacing the intended "Studio-newest-first" customer-facing order (a second real defect found on review, not just under-description). | High | High (confirmed by owner, reproduced in tests) | Fixed, corrected on review: (1) new compact `generated/portal-catalog/v{version}/filters/tags-facet.json` asset (tags with ≥1 ready design + count only, 256 KiB budget); `filters.tagFacetPath` added to the manifest (additive, schema version unchanged); (2) the Firestore fallback was removed entirely (owner-approved decision — no correct complete bounded fallback exists) in favor of a graceful "unavailable" UI state; (3) new pure `portalCatalogBrowseOrder`/`planPortalCatalogSearchPage` preserve the correct "Studio-newest-first, design-ID-descending-tiebreaker" order (matching the existing Firestore browse convention) through intersection and pagination, verified with a dedicated order-preservation test plus the exact "BEST" regression test. `portalCatalogAssetService.ts` has no Firestore import at all (structural proof of zero Firestore reads on this path). 37 new/updated regression tests total. `npm run build:portal` confirmed exit 0 (a prior report of "did not complete" was a tool-timeout artifact, not a real failure). Owner redeployed and republished generation 9 successfully, but local QA still failed with the same
symptoms. Root cause proven (live outside-browser diagnostic + owner's browser console error):
**Storage bucket CORS**, not a code/asset defect — `https://myprintrequest.dev` was blocked from
reading `firebasestorage.googleapis.com` responses for the (correctly public, per Storage Rules)
generated assets. Corrected the repo's CORS config file/doc, which had targeted the wrong bucket
alias (`fresh-prints-dev.appspot.com`, confirmed 404/nonexistent) instead of the real live bucket
(`fresh-prints-dev.firebasestorage.app`). Independently found and fixed a second real defect:
`useCatalogDesigns.ts` silently fell through to an unrelated, unfiltered Firestore page for
search/multi-tag queries on any generated-asset failure (including a CORS error) — removed that
fallback entirely for a graceful "unavailable" state; normal browse's separate, bounded fallback is
unchanged. Reviewed Studio's search/tag-filter code for parity: ordering, substring search, and
tag-count scoping already correctly match Portal's existing conventions — no parity defects found.
Owner applied the corrected CORS configuration — confirmed working: generated assets now load, and
searching "best" correctly returns both matches immediately. Follow-up refinement requested: the tag
modal did not dynamically narrow after selecting a tag (unrelated tags stayed visible with stale
global counts) — a feature gap in the original design, not a regression. Fixed via Option A (existing
per-tag design-ID list asset + existing card-bucket assets are sufficient; no new generated asset, no
manifest change, zero Firestore reads, zero fetch per candidate tag) — new
`portalCatalogAssetService.listNarrowedTagFacets` with pure, unit-tested helpers, wired into
`CatalogTagFilterModal`. 7 new tests (99 total across the goal). **Do not close until:** owner reviews
this refinement and confirms via local retest that tag selection correctly narrows to AND-filtered
results with live counts, clearing restores the global list, and zero Firestore reads occur throughout.
No further Functions redeployment, snapshot republish, or CORS change is required. | Owner/Review |
open, CORS fix confirmed working; dynamic tag-facet narrowing implemented, pending owner review/retest |
| R-016 | Owner decided to move Fresh Prints Studio's Design Library (normal ready browse, search, category/tag/halftone filtering, dynamic tag narrowing, pagination) off Firestore onto the same generated-catalog architecture Portal now uses, without changing Studio's existing UX. Cold-entry Studio Design Library previously cost ~1,122 tag-collection reads (paged to completion regardless of tag count, the single largest cost) + ~200 category reads + ~100 design-page reads, all cached but repeated on every session restart/cache expiry. | Medium | Confirmed via direct code inspection (not assumed) | Full Plan/Review amendment completed and approved: Option B (reuse existing public Portal card buckets + client-safe taxonomy; add one new compact asset `generated/portal-catalog/v{contentVersion}/studio/ready-index.json` carrying `id/title/description/categoryId/tags/updatedAtMs` per ready design in Studio's own `updatedAt DESC, id DESC` order, independent of Portal's `createdAt`-based order); Electron **main-process IPC transport** (new `catalogAsset` IPC feature) instead of browser CORS, since packaged Electron's `file://` renderer origin cannot safely be added to bucket CORS; archived designs remain entirely Firestore-only (never enter the public asset — proven via a new rules-emulator test, not assumed); owner approved making dynamic tag narrowing catalog-wide accurate (an intentional fix to an existing loaded-pages-only limitation). Implemented: shared type/parser/manifest field, publisher asset write, Electron IPC bridge, Studio consumer service, `useGeneratedReadyDesigns` hook, and `DesignLibraryPage.tsx` wiring that reuses every one of Studio's existing pure filter/search/narrowing functions unchanged against the new data source. Archived mode, detail/edit (always re-fetches authoritative Firestore data), and request-selection mode preserved. Owner republished generation 38, validated it live (correct at the time), then found via QA that the catalog reshuffled on print-request/show/edit activity and ~1,300 Firestore reads occurred. Root cause: the generated ordering field (`updatedAtMs`) is bumped by exactly that activity. **Corrected (owner decision):** ordering now uses the immutable `createdAt` field (`PortalCatalogStudioReadyIndex.createdAtMs`, `createdAt DESC, id DESC`) — confirmed via repo investigation that every design-creation path writes `createdAt` unconditionally and Firestore rules forbid changing it post-creation; no backfill needed. The ~1,300 reads were attributed (not newly caused): `useCatalogTags`'s full tag-collection pagination (~1,122 reads) + `useCategories()`'s bounded load (≤200 reads), both pre-existing and unconditional on Design Library mount, unchanged since before this Studio work began. Surfaced (not fixed) that `useCategories()` should already use the generated client-safe taxonomy per the original Plan — a real, separate follow-up. Owner then directed closing that gap: converted `DesignLibraryPage.tsx`'s normal-mode `categories`/`catalogTags` to the existing, already-published `generated/catalog-reference/**` client-safe taxonomy snapshot (same one Portal publishes/consumes — no new asset, no manifest/publisher change, no redeploy/republish needed for this part). Confirmed by direct inspection that only `id/name/sortOrder/isActive`/`id/name/aliases/status` are ever read by the Design Library's own filter/dropdown/tag-picker logic; `CategoryManagementModal`/`TagManagementModal` (real management flows) stay on full Firestore-backed data unchanged. One owner-approved narrow behavior change (via `AskUserQuestion`): tag-modal search no longer matches each tag's `preferredWhen` guidance text (server-only, excluded from the public snapshot) — name/alias matching is unaffected. 36 new/updated tests total across all three passes (155 total across the goal), rules 8/8 (unaffected), functions/Portal/full-Studio builds all exit 0. **Do not close until:** owner approves redeploying `rebuildCatalogSnapshots`/`onPortalCatalogSnapshotSourceWritten` again (live generation 38 still has the old `updatedAtMs` ordering — the taxonomy fix needs no redeploy), republishes once, and completes a real Studio dev-session retest (this environment has no Electron/browser automation tooling to perform it — manual script provided). No Storage Rules, Firestore rules, or bucket CORS change required. | Owner/Review | open, ordering corrected, reads attributed, and taxonomy read gap closed; pending owner Functions redeploy/republish approval (ordering fix only) and Studio retest |


---

## Closed Risks

| ID | Risk | Resolution | Closed date |
|----|------|------------|-------------|
| R-C01 | AppForge migration incomplete / stale paths in entry docs | Migration + intake verification | 2026-06-24 |
| R-C02 | Project docs were AppForge templates only | Intake populated PROJECT_HEALTH, TECH_DEBT, INTAKE_FINDINGS | 2026-06-24 |
| R-C03 | Etsy Open API relevance vs website search for Phase 9A | Reopened/accepted as residual recall risk with link-first fallback (ADR-FP-087l) | 2026-07-16 |
| R-013 | AI catalog reference snapshot exceeded its original 256 KiB budget at the real ~1,122-tag dev corpus (measured 295,152 bytes) | Owner-approved AI-private budget raised to 512 KiB (ADR-FP-120 amendment); redeployed and confirmed live: `generated/catalog-reference/manifest.json` publicly readable, `generation: 4`, `contentVersion: "4-1a810751ceb2b381"` matching the callable result; `generated/catalog-reference/ai/v4-1a810751ceb2b381.json` confirmed private (403 on unauthenticated read/metadata) and confirmed uploaded (client sibling asset at the same version exists); `generated/catalog-reference/client/v4-1a810751ceb2b381.json` confirmed publicly readable and unauthenticated-write-denied. Exact live AI asset byte size not independently re-measured post-publish (would require Admin credentials, out of policy for this session) but is expected ≈295 KB at unchanged taxonomy content, well under the 512 KiB ceiling and the 409,600-byte warning threshold. | 2026-07-24 |
| R-014 | Portal catalog root manifest exceeded its 32 KiB budget (measured 134,069 bytes, 4.09x over) from full path enumeration | Fixed with deterministic path templates (manifest schema v2); redeployed and confirmed live via unauthenticated public reads: `generated/portal-catalog/manifest.json` is `schemaVersion: 2`, `generation: 4`, `contentVersion: "4-e0e5b3ae9fb69797"` (matches the callable result exactly), measures 3,214 bytes (9.8% of budget) with real category IDs/300+ shard keys; representative targets confirmed live — Discover (200), recent page 0 (200), category filter with 45 real design IDs (200), category page 0 (200), card bucket 1 at 2,238 bytes (200; bucket 0/3/4 correctly 404 as legitimately empty at this catalog size), search shard "ab" (200); unauthenticated write to the manifest confirmed denied (403); unauthenticated read of `snapshotPublicationState/catalog-reference` confirmed denied (403, Firestore REST), consistent with the rules suite. Orphaned v1/v2/v3 catalog-reference client assets from earlier failed attempts confirmed to still exist (harmless, unreferenced, retained per instructions — not deleted). | 2026-07-24 |

---

## Revision History

| Date | Summary |
|------|---------|
| 2026-07-24 | R-016: closed the taxonomy read gap. Converted Design Library categories/tags to the existing generated client-safe taxonomy (no new asset, no redeploy needed). Confirmed by inspection which fields are actually read; kept management modals (Category/Tag Management) on full Firestore data. Owner-approved dropping `preferredWhen` tag-search matching. 7 new tests (155 total). |
| 2026-07-24 | R-016: owner republished generation 38 and validated it, then Studio QA found catalog reshuffling on unrelated activity plus ~1,300 reads. Root cause: generated ordering used `updatedAtMs`, bumped by request/show/edit writes. Corrected to owner-decided `createdAt`-based ordering after confirming createdAt is immutable/always-written (no backfill needed). Attributed the ~1,300 reads to pre-existing, unconverted `useCatalogTags`/`useCategories` Firestore loads (not a new regression); surfaced but did not fix an already-approved Plan gap (categories should use generated taxonomy). 10 more tests (148 total). Pending owner Functions redeploy/republish approval and Studio retest. |
| 2026-07-24 | R-016 added and implemented — Studio Design Library moved to the generated-catalog architecture per an approved Plan/Review amendment (Option B, Electron IPC transport, owner-approved catalog-wide tag narrowing). New `studio/ready-index.json` asset, `catalogAsset` IPC bridge, `useGeneratedReadyDesigns` hook, `DesignLibraryPage.tsx` wiring reusing Studio's existing filter logic unchanged. 19 new tests (138 total), rules 8/8. Pending owner Functions redeploy/republish approval and a Studio retest (no automation tooling available in this environment to perform it). |
| 2026-07-24 | R-015: owner-applied CORS fix confirmed working (assets load, "best" search fixed). Follow-up: tag modal didn't narrow after selection (stale global counts on unrelated tags) — a feature gap, not a regression. Fixed with zero new generated assets (existing per-tag design-ID list + card-bucket assets are sufficient for exact AND-narrowed co-occurrence counts, zero Firestore reads, zero fetch per candidate tag). 7 new tests (99 total). Pending owner review/retest; no redeploy/republish/CORS change needed. |
| 2026-07-24 | R-015: generation-9 owner QA still failed after redeploy/republish. Root cause proven via a live outside-browser diagnostic + the owner's exact browser console error: Storage bucket CORS, not a code/asset defect. Corrected the repo's CORS config (previously targeted the wrong, nonexistent bucket alias). Independently fixed a second real defect — an unsafe Firestore fallback for search/multi-tag that fired on any generated-asset failure — removed entirely for a graceful unavailable state. Reviewed Studio parity (ordering/search/tag-counts): already correct, no changes needed. Pending owner CORS approval + browser retest; no further redeploy/republish required. |
| 2026-07-24 | R-015 review pass: found the prior fix's Firestore "fallback" was still unbounded (full `tags`+`designs` scan) and its search fix sorted alphabetically instead of preserving the intended newest-first order — both corrected (fallback removed per owner decision; order-preserving `portalCatalogBrowseOrder` added). `npm run build:portal` confirmed exit 0 (prior "did not complete" was a tool-timeout artifact). 11 more tests (37 total for R-015). Still open pending redeploy of the corrections, republish, and a real browser-based retest (no automation tooling available in this environment to perform it). |
| 2026-07-24 | R-015 added — post-publication owner Portal QA found the tag modal showing the full taxonomy with no counts and a search-pagination regression (BEST query), plus a ~3,600-read Firestore spike; root causes diagnosed and fixed (new tag-facet generated asset, deterministic search-result ordering before pagination, rebounded Firestore fallback); 26 new tests; not yet closed pending redeploy/republish/local retest |
| 2026-07-24 | R-013 and R-014 closed — owner successfully redeployed both fixes and ran `rebuildCatalogSnapshots` once; both `catalog-reference` (gen 4) and `portal-catalog` (gen 4) published successfully. Live confirmed via unauthenticated public Storage/Firestore reads: correct public/private access, correct write denial, correct manifest content/sizes, representative generated assets addressable. |
| 2026-07-24 | R-014 added — after the R-013 redeploy, retry surfaced a second budget failure on `generated/portal-catalog/manifest.json` (130.9 KB, over 4x the 32 KiB budget) caused by full path enumeration; fixed with deterministic path templates (manifest schema v2), corrected size 2.13 KB; implementation correction under the existing approved architecture, no Plan/Review amendment needed; not yet closed pending live redeploy/retry |
| 2026-07-23 | R-013 remediation: owner approved raising only the AI-private reference snapshot budget to 512 KiB (no sharding, no other budget change); implemented with an 80% non-blocking warning and regression tests; not yet closed pending live redeploy/retry confirmation |
| 2026-07-23 | R-013 added — first real `rebuildCatalogSnapshots` dev invocation proved the AI catalog reference snapshot exceeds its unsharded 256 KiB budget at Fresh Prints Dev's actual tag count; blocks `catalog-reference` publication until a reviewed sharding/budget/content decision is made |
| 2026-07-23 | R-012 added — `sharp@0.33.5` libvips advisory reviewed for Wave C dev deployment checkpoint; DoS-only EXIF path reachable via customer uploads, not introduced by Wave C, fix deferred to a separate reviewed major-version upgrade |
| 2026-07-16 | ADR-FP-087l restores Open API listings under link-first UI; R-C03 residual recall accepted with fallback links |
| 2026-07-16 | R-011 provider email idempotency-window residual risk documented (ADR-FP-089) |
| 2026-07-16 | R-010 mitigated — owner rejected scrape; ADR-FP-087j link-only rip |
| 2026-07-16 | R-010 live vendor returned Firecrawl → ScraperAPI (ADR-FP-087i); secret `SCRAPERAPI_API_KEY` |
| 2026-07-16 | R-010 live vendor switched ScraperAPI → Firecrawl (ADR-FP-087h); secret `FIRECRAWL_API_KEY` |
| 2026-07-15 | R-010 owner-approved scrape via ScraperAPI (dev); R-C03 Open API rip |
| 2026-07-15 | R-010 scrape ToS gate; R-C03 Open API rip |
| 2026-07-12 | R-008 dual assets / R-009 frozen upload catalog status after promote; R-007 notes cleanup + wipe target |
| 2026-07-11 | R-007 public customer artwork upload threat model (Phase 8 fast-follow) |
| 2026-06-24 | Fresh Prints intake risks added; starter template risks closed |
